---
phase: 15-deploy-and-finalize-qr-codes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/flyer/qr_app.png
  - docs/flyer/qr_feedback.png
  - docs/flyer/facility_flyer.pdf
  - docs/PILOT_LINKS.md
  - .planning/REQUIREMENTS.md
autonomous: false

requirements:
  - APP-05
  - PRES-02
  - PRES-04

must_haves:
  truths:
    - "caregiver_app.py is accessible at a live Streamlit Cloud HTTPS URL"
    - "docs/flyer/qr_app.png encodes the real deployed app URL, not a placeholder"
    - "A Google Form for pilot feedback exists with at least one test response"
    - "docs/flyer/qr_feedback.png encodes the real Google Form URL, not a placeholder"
    - "docs/flyer/facility_flyer.pdf has been regenerated with both real QR codes embedded"
    - "The deployed app URL and form URL are documented in docs/PILOT_LINKS.md"
  artifacts:
    - path: "docs/flyer/qr_app.png"
      provides: "QR code encoding the live Streamlit Cloud URL"
      contains: "non-placeholder PNG (>446 bytes)"
    - path: "docs/flyer/qr_feedback.png"
      provides: "QR code encoding the Google Form URL"
      contains: "non-placeholder PNG, not byte-for-byte identical to qr_app.png"
    - path: "docs/flyer/facility_flyer.pdf"
      provides: "Print-ready one-page flyer with real QR codes"
      contains: "PDF rebuilt after QR regeneration"
    - path: "docs/PILOT_LINKS.md"
      provides: "Single source of truth for deployed URLs"
      contains: "Streamlit Cloud URL and Google Form URL"
  key_links:
    - from: "scripts/generate_qr_codes.py"
      to: "docs/flyer/qr_app.png"
      via: "--app-url argument"
      pattern: "qr\\.add_data\\(url\\)"
    - from: "scripts/generate_qr_codes.py"
      to: "docs/flyer/qr_feedback.png"
      via: "--form-url argument"
      pattern: "qr\\.add_data\\(url\\)"
    - from: "scripts/generate_flyer_pdf.py"
      to: "docs/flyer/facility_flyer.pdf"
      via: "Image(str(QR_APP)) and Image(str(QR_FORM))"
      pattern: "Image\\(str\\(qr_path\\)\\)"
---

<objective>
Close the three remaining CSEF presentation gaps: deploy the caregiver app to Streamlit Cloud,
create the pilot feedback Google Form, then regenerate both QR codes and the facility flyer PDF
with real URLs.

Purpose: A judge or facility visitor who scans the QR code on the poster must reach a live app.
Without this, the "Judge scans QR → opens live caregiver app" E2E flow is broken at the first step.

Output: Live deployed app URL + real QR codes + regenerated PDF + PILOT_LINKS.md documenting both URLs.
</objective>

<execution_context>
@/Users/amaarchughtai/.claude/get-shit-done/workflows/execute-plan.md
@/Users/amaarchughtai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-caregiver-app-and-pilot-preparation/13-VERIFICATION.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Deploy to Streamlit Cloud and create Google Form</name>
  <files>none — external service actions only</files>
  <what-built>
    App code is complete and all model artifacts are git-tracked (EEGNet 34KB + TCN 128KB +
    scalers 2KB — well within Streamlit Cloud limits). Scripts to regenerate QR codes and the
    flyer PDF exist and are ready. Only the two external service URLs are missing.
  </what-built>
  <action>
    HUMAN ACTION REQUIRED — no CLI/API exists for these steps:

    Step A — Deploy to Streamlit Cloud:
    1. Go to https://share.streamlit.io and sign in with your GitHub account
    2. Click "New app"
    3. Repository: closedloop-40hz-entrainment | Branch: CSEF | Main file: caregiver_app.py
    4. Click "Deploy" and wait for the build to succeed (typically 2-3 minutes)
    5. Copy the full HTTPS URL (format: https://SOMETHING.streamlit.app)
    6. Open the URL on your phone — verify the NeuroCare 40Hz welcome page loads

    Step B — Create the Pilot Feedback Google Form:
    1. Go to https://forms.google.com (use amaardevx@gmail.com)
    2. Create a new form titled "NeuroCare 40Hz — Pilot Feedback"
    3. Add these 5 questions:
       - Q1: "Your role" (Multiple choice: Caregiver / Facility Staff / Family Member / Other)
       - Q2: "How easy was the app to use?" (Linear scale 1-5, label 1=Very Difficult, 5=Very Easy)
       - Q3: "Would you use this with a patient?" (Multiple choice: Yes / No / Maybe)
       - Q4: "What would you improve?" (Short answer)
       - Q5: "May we follow up with you?" (Multiple choice: Yes / No)
    4. Click the "Send" button and copy the respondent link (format: https://forms.gle/XXXXXXXX)
    5. Submit one test response to confirm the form works
  </action>
  <verify>Human confirms both URLs are live and functional before proceeding</verify>
  <done>
    Both a live Streamlit Cloud URL and a live Google Form URL exist and have been shared.
    Test response submitted to the form.
  </done>
  <resume-signal>
    Paste both URLs in the format:
    App URL: https://YOUR-APP.streamlit.app
    Form URL: https://forms.gle/XXXXXXXX
  </resume-signal>
</task>

<task type="auto">
  <name>Task 2: Regenerate QR codes, rebuild flyer PDF, and document URLs</name>
  <files>
    docs/flyer/qr_app.png,
    docs/flyer/qr_feedback.png,
    docs/flyer/facility_flyer.pdf,
    docs/PILOT_LINKS.md,
    .planning/REQUIREMENTS.md
  </files>
  <action>
    Using the URLs provided in Task 1, execute the following steps in order:

    1. Activate virtualenv and regenerate QR codes:
       source venv/bin/activate
       python scripts/generate_qr_codes.py \
         --app-url "APP_URL_FROM_TASK_1" \
         --form-url "FORM_URL_FROM_TASK_1"
       Both PNGs will be written to docs/flyer/. Confirm [PASS] output for each.

    2. Verify the QR files are no longer placeholders:
       - Both files must exist and be larger than 446 bytes
       - qr_app.png and qr_feedback.png must NOT be byte-for-byte identical
       Run: python scripts/generate_qr_codes.py --check

    3. Rebuild the flyer PDF with the real QR codes now in place:
       python scripts/generate_flyer_pdf.py
       Confirm [PASS] output and that docs/flyer/facility_flyer.pdf is regenerated.

    4. Create docs/PILOT_LINKS.md as the single source of truth for deployed URLs.
       Content must include:
       - Deployed Streamlit Cloud URL
       - Google Form respondent URL
       - Date deployed (2026-03-21 or actual date)
       - Git branch deployed from (CSEF)
       Use this format:
       ```
       # Pilot Links

       **Deployed app:** https://YOUR-APP.streamlit.app
       **Feedback form:** https://forms.gle/XXXXXXXX
       **Deployed:** YYYY-MM-DD from branch CSEF
       ```

    5. Fix REQUIREMENTS.md checkboxes — change these three from [ ] to [x]:
       - APP-05: "[ ] **APP-05**" -> "[x] **APP-05**"
       - PRES-02: "[ ] **PRES-02**" -> "[x] **PRES-02**"
       - PRES-04: "[ ] **PRES-04**" -> "[x] **PRES-04**"
       Also update the traceability table rows for APP-05, PRES-02, PRES-04 from "Pending" to "Complete".
  </action>
  <verify>
    <automated>
      source venv/bin/activate && python scripts/generate_qr_codes.py --check && python -m py_compile scripts/generate_qr_codes.py scripts/generate_flyer_pdf.py && python -c "a=open('docs/flyer/qr_app.png','rb').read(); b=open('docs/flyer/qr_feedback.png','rb').read(); assert len(a)>446 and len(b)>446 and a!=b, 'QR placeholder check failed'; print('[PASS] QR codes valid')" && grep -q '\[x\].*APP-05' .planning/REQUIREMENTS.md && echo '[PASS] REQUIREMENTS.md updated'
    </automated>
  </verify>
  <done>
    - qr_app.png encodes the real Streamlit Cloud URL (file larger than 446 bytes, different from qr_feedback.png)
    - qr_feedback.png encodes the real Google Form URL (different content from qr_app.png)
    - facility_flyer.pdf regenerated after QR update
    - docs/PILOT_LINKS.md exists with both URLs documented
    - REQUIREMENTS.md marks APP-05, PRES-02, PRES-04 as [x] Complete
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify QR codes work on a real phone</name>
  <files>docs/flyer/qr_app.png, docs/flyer/qr_feedback.png, docs/flyer/facility_flyer.pdf</files>
  <what-built>
    Real QR codes generated from the deployed Streamlit Cloud URL and Google Form URL,
    embedded in a regenerated facility_flyer.pdf.
  </what-built>
  <action>
    Human scans both QR codes with a phone camera to confirm they resolve to live URLs.
    Cannot be automated — requires a physical phone and live HTTP access.
  </action>
  <how-to-verify>
    1. Open docs/flyer/qr_app.png on your screen (or print the PDF)
    2. Scan with iOS/Android camera app
    3. Expected: phone browser opens the live NeuroCare 40Hz caregiver app
    4. Navigate to a patient profile to confirm the app is fully functional (not just the landing page)

    5. Open docs/flyer/qr_feedback.png and scan it
    6. Expected: phone browser opens the Google Form you created
    7. Confirm the 5 questions are present

    8. Open docs/flyer/facility_flyer.pdf — verify both QR codes are visible (not grey placeholder boxes)
  </how-to-verify>
  <verify>Human confirms QR codes resolve to live URLs on a phone</verify>
  <done>Both QR codes scan successfully on a phone and resolve to the correct live URLs</done>
  <resume-signal>Type "approved" if both QR codes work, or describe what's broken</resume-signal>
</task>

</tasks>

<verification>
Run after all tasks complete:

```bash
source venv/bin/activate
python scripts/generate_qr_codes.py --check
python -c "
a = open('docs/flyer/qr_app.png','rb').read()
b = open('docs/flyer/qr_feedback.png','rb').read()
assert len(a) > 446, f'qr_app.png still placeholder size: {len(a)}'
assert len(b) > 446, f'qr_feedback.png still placeholder size: {len(b)}'
assert a != b, 'QR files are identical -- both encode same URL'
print('[PASS] QR codes are distinct and non-placeholder')
"
python -c "
import os
pdf_size = os.path.getsize('docs/flyer/facility_flyer.pdf')
assert pdf_size > 6569, f'PDF not larger than original placeholder-QR version: {pdf_size}'
print(f'[PASS] facility_flyer.pdf size: {pdf_size} bytes')
"
grep '\[x\].*APP-05' .planning/REQUIREMENTS.md && echo '[PASS] APP-05 marked complete'
grep '\[x\].*PRES-02' .planning/REQUIREMENTS.md && echo '[PASS] PRES-02 marked complete'
grep '\[x\].*PRES-04' .planning/REQUIREMENTS.md && echo '[PASS] PRES-04 marked complete'
test -f docs/PILOT_LINKS.md && echo '[PASS] PILOT_LINKS.md exists'
```
</verification>

<success_criteria>
- caregiver_app.py is deployed and accessible at a live Streamlit Cloud HTTPS URL
- A judge can scan qr_app.png and open the live app on their phone without explanation
- qr_feedback.png encodes a real Google Form URL with 5 structured questions and at least one test response
- docs/flyer/facility_flyer.pdf is regenerated with real QR codes visible (no grey placeholder boxes)
- docs/PILOT_LINKS.md documents both URLs as the single source of truth
- REQUIREMENTS.md correctly marks APP-05, PRES-02, PRES-04 as [x] Complete
- The "Judge scans QR -> live app" E2E flow in the milestone audit is resolved
</success_criteria>

<output>
After completion, create `.planning/phases/15-deploy-and-finalize-qr-codes/15-01-SUMMARY.md`
</output>
