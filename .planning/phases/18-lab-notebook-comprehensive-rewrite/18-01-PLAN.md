---
phase: 18-lab-notebook-comprehensive-rewrite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CSEF/Lab Notebook/P10_Lab_Notebook_VFINAL.md
  - CSEF_presentation/notebook/v1_lab_notebook_extension.md
  - CSEF_presentation/notebook/P10_Lab_Notebook_COMPLETE.pdf
autonomous: true
requirements: []

must_haves:
  truths:
    - "Zero em-dashes in both notebook files"
    - "Every code snippet parameter matches actual codebase values"
    - "Hysteresis documented as 5s with poster discrepancy noted"
    - "TCN parameter counts labeled by feature set (31043/73-feat, 22914/12-feat h64, 5154/12-feat h32)"
    - "72.1% alignment attributed to 73-feature model explicitly"
    - "Horizon sweep persistence values match horizon_sweep_pac_stim.json"
    - "Fortunato [8] reference present"
    - "PDF generates without errors"
    - "Tonal consistency across March boundary (no voice break)"
  artifacts:
    - path: "CSEF/Lab Notebook/P10_Lab_Notebook_VFINAL.md"
      provides: "Original notebook (Jan 15 - Mar 22)"
      contains: "Fortunato"
    - path: "CSEF_presentation/notebook/v1_lab_notebook_extension.md"
      provides: "Extension notebook (Mar 24 - Apr 8)"
      contains: "onset_tau_sec"
    - path: "CSEF_presentation/notebook/P10_Lab_Notebook_COMPLETE.pdf"
      provides: "Combined PDF notebook for CSEF judging"
  key_links:
    - from: "CSEF/Lab Notebook/P10_Lab_Notebook_VFINAL.md"
      to: "src/tribe_v2/neural_mass.py"
      via: "code snippet tau_e=0.004"
      pattern: "tau_e.*0\\.004"
    - from: "CSEF_presentation/notebook/v1_lab_notebook_extension.md"
      to: "src/tribe_v2/cortical_model.py"
      via: "code snippet onset_tau_sec=1.5"
      pattern: "onset_tau_sec.*1\\.5"
    - from: "CSEF_presentation/notebook/v1_lab_notebook_extension.md"
      to: "experimental/results/horizon_sweep_pac_stim.json"
      via: "persistence R2 values at each horizon"
      pattern: "0\\.104"
---

<objective>
Validate both lab notebook files against all audit dimensions, fix any remaining issues found during validation, and regenerate the final combined PDF.

Purpose: The notebook must score 95+ on all audit dimensions (AI voice, clinical depth, code alignment, poster consistency) before CSEF judging on April 12. The heavy rewriting was completed earlier today -- this plan runs the systematic validation, fixes any remaining gaps, and produces the final PDF artifact.

Output: Validated P10_Lab_Notebook_VFINAL.md + v1_lab_notebook_extension.md + regenerated P10_Lab_Notebook_COMPLETE.pdf
</objective>

<execution_context>
@/Users/amaarchughtai/.claude/get-shit-done/workflows/execute-plan.md
@/Users/amaarchughtai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-lab-notebook-comprehensive-rewrite/18-CONTEXT.md
@.planning/phases/18-lab-notebook-comprehensive-rewrite/18-RESEARCH.md
@.planning/phases/18-lab-notebook-comprehensive-rewrite/18-VALIDATION.md

@CSEF/Lab Notebook/P10_Lab_Notebook_VFINAL.md
@CSEF_presentation/notebook/v1_lab_notebook_extension.md
@CSEF_presentation/notebook/generate_notebook_pdf.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Systematic validation of both notebook files against all audit dimensions</name>
  <files>CSEF/Lab Notebook/P10_Lab_Notebook_VFINAL.md, CSEF_presentation/notebook/v1_lab_notebook_extension.md</files>
  <action>
Run a comprehensive validation pass across both notebook files. For each check below, record PASS or FAIL with specific line references. Fix any FAILs in-place.

**Em-dash check (LOCKED: zero em-dashes):**
```bash
grep -c '\xe2\x80\x94' "CSEF/Lab Notebook/P10_Lab_Notebook_VFINAL.md"
grep -c '\xe2\x80\x94' "CSEF_presentation/notebook/v1_lab_notebook_extension.md"
```
Both must return 0. If any found, replace with --.

**Code parameter cross-reference (LOCKED: must match actual codebase):**
- Verify tau_e=0.004 in extension matches `src/tribe_v2/neural_mass.py`
- Verify onset_tau_sec=1.5 in extension matches `src/tribe_v2/cortical_model.py`
- Verify hold_time_sec=5.0 in extension matches `config.yaml`
- Verify theta_band=(4.0, 8.0), gamma_band=(38.0, 42.0), n_bins=18 in original matches `src/pac_computation.py`
- For each: grep the codebase file, grep the notebook file, confirm match.

**Poster consistency (LOCKED: hysteresis = 5s, discrepancy noted):**
- Confirm the original notebook says "5-second hysteresis" (not 3s)
- Confirm the extension has the config fix entry mentioning "was 3.0"
- Confirm 72.1% alignment is explicitly attributed to 73-feature model (check Feb 26 entry AND the March 22 note)

**TCN parameter labeling (LOCKED: each variant labeled):**
- 31,043 params with 73 features (Feb 17 entry)
- 22,914 params with 12 features h=64 (March 10-12 entry)
- 5,154 params with 12 features h=32 (March 13-14 entry)

**Number cross-references:**
- Horizon sweep Feb 19 table: these are 73-feature numbers. Verify the note says so.
- Horizon sweep March 15-17 table: these are 12-feature numbers from horizon_sweep_pac_stim.json. Cross-check persistence at h=5 = 0.104 (from JSON: persist_r2 for horizon=5).
- Controller results (72.1%, 82.6%, 30.5, 91%): verify these appear accurately.
- Feature ablation: test R2 from -0.025 (73-feat) to 0.606 (12-feat 5-seed mean).

**Fortunato reference [8] (LOCKED: must be present):**
Confirm [8] Fortunato appears in the References section of P10_Lab_Notebook_VFINAL.md.

**Tonal consistency across March boundary:**
- Read the last paragraph of the Feb 26 entry and the first paragraph of the "CSEF Preparation -- March 2026" section
- Verify the voice doesn't shift from informal/student to formal/paper or vice versa
- The March 3-5 entry should maintain the same conversational tone as January-February entries
- Check for AI voice patterns: no parallel structure lists, no "leveraging", no "comprehensive", no hedging qualifiers
- If any tonal breaks found, rewrite the transitional paragraphs to maintain consistent student voice

**Imperfection quotient (target: 5+ genuine corrections/updates/failed attempts):**
Count instances of: corrections ("UPDATE:", typo acknowledgments), failed attempts (SpecTempNet leakage, Muse BLE failure, TRIBE V2 WhisperX failure), confusion/frustration language, gap acknowledgments. Must find at least 5 across both files.

**Clinical depth:**
Confirm at least one entry engages with molecular pathways (microglia, amyloid, gamma mechanism). The Jan 15 entry should mention the Iaccarino microglia/amyloid mechanism.

For any FAIL: make the minimal fix directly in the markdown file. Do NOT rewrite entire entries -- surgical fixes only.
  </action>
  <verify>
    <automated>bash -c 'E1=$(grep -c "—" "CSEF/Lab Notebook/P10_Lab_Notebook_VFINAL.md" 2>/dev/null || echo 0); E2=$(grep -c "—" "CSEF_presentation/notebook/v1_lab_notebook_extension.md" 2>/dev/null || echo 0); T1=$(grep -c "tau_e.*0.004" "CSEF_presentation/notebook/v1_lab_notebook_extension.md"); T2=$(grep -c "onset_tau_sec.*1.5" "CSEF_presentation/notebook/v1_lab_notebook_extension.md"); F=$(grep -c "Fortunato" "CSEF/Lab Notebook/P10_Lab_Notebook_VFINAL.md"); H=$(grep -c "5-second hysteresis\|5.0.*hold_time\|hold_time_sec: 5" "CSEF/Lab Notebook/P10_Lab_Notebook_VFINAL.md"); echo "em-dash-orig=$E1 em-dash-ext=$E2 tau_e=$T1 onset_tau=$T2 fortunato=$F hysteresis=$H"; if [ "$E1" = "0" ] && [ "$E2" = "0" ] && [ "$T1" -ge 1 ] && [ "$T2" -ge 1 ] && [ "$F" -ge 1 ] && [ "$H" -ge 1 ]; then echo "ALL CHECKS PASS"; exit 0; else echo "SOME CHECKS FAILED"; exit 1; fi'</automated>
  </verify>
  <done>All validation dimensions pass. Zero em-dashes, code params match codebase, poster alignment correct, TCN params labeled, Fortunato [8] present, tonal consistency maintained, 5+ imperfections documented, clinical context present. Any fixes applied surgically.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate final PDF and verify output</name>
  <files>CSEF_presentation/notebook/P10_Lab_Notebook_COMPLETE.pdf</files>
  <action>
Run the PDF generator to produce the final combined notebook PDF.

```bash
python3 CSEF_presentation/notebook/generate_notebook_pdf.py
```

After generation, verify:
1. The script completes without errors
2. The output file exists at CSEF_presentation/notebook/P10_Lab_Notebook_COMPLETE.pdf
3. Report the page count from the script output
4. Verify file size is reasonable (should be several MB due to embedded figures)

If the generator fails:
- Check that all figure paths referenced in generate_notebook_pdf.py exist (results/figures/*.png, results/tribe_v2/*.png, results/figures/ai_generated/*.png)
- Fix any missing figure references by commenting them out or providing fallback paths
- Re-run until successful

Do NOT modify the content of the notebook files in this task -- that was Task 1's job.
  </action>
  <verify>
    <automated>bash -c 'python3 CSEF_presentation/notebook/generate_notebook_pdf.py 2>&1 && test -f CSEF_presentation/notebook/P10_Lab_Notebook_COMPLETE.pdf && echo "PDF GENERATED SUCCESSFULLY" || echo "PDF GENERATION FAILED"'</automated>
  </verify>
  <done>P10_Lab_Notebook_COMPLETE.pdf exists, was generated without errors, contains all notebook content from both files with embedded figures, and is ready for CSEF judging.</done>
</task>

</tasks>

<verification>
Run the full validation battery from 18-VALIDATION.md:

```bash
# Em-dash check (both must return 0)
grep -c '—' "CSEF/Lab Notebook/P10_Lab_Notebook_VFINAL.md"
grep -c '—' "CSEF_presentation/notebook/v1_lab_notebook_extension.md"

# Code params match codebase
grep "tau_e" src/tribe_v2/neural_mass.py | head -2
grep "onset_tau" src/tribe_v2/cortical_model.py | head -2

# PDF regenerates cleanly
python3 CSEF_presentation/notebook/generate_notebook_pdf.py
```

All commands must succeed. Em-dash counts must be 0. PDF must generate without errors.
</verification>

<success_criteria>
- Both notebook files pass all 8 validation dimensions (em-dash, code alignment, poster consistency, TCN params, number accuracy, Fortunato ref, tonal consistency, imperfection quotient)
- P10_Lab_Notebook_COMPLETE.pdf regenerated successfully with all figures embedded
- No remaining audit issues that would score below 95 on any dimension
</success_criteria>

<output>
After completion, create `.planning/phases/18-lab-notebook-comprehensive-rewrite/18-01-SUMMARY.md`
</output>
