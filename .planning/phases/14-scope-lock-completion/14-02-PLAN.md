---
phase: 14-scope-lock-completion
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - results/4ch_vs_7ch_r2_gap_report.md
autonomous: true
requirements:
  - RSRCH-05

must_haves:
  truths:
    - "A formal 4ch vs 7ch R² gap report exists in results/ that a CSEF judge can read"
    - "The report correctly cites R² for EEGNet 4ch (0.016) vs 7ch (0.287) and TCN 4ch (val 0.447, test 0.156) vs 7ch baseline"
    - "The report documents caveats about non-comparable PAC labels and deployment domain gap"
  artifacts:
    - path: "results/4ch_vs_7ch_r2_gap_report.md"
      provides: "Formal performance comparison between 4-channel and 7-channel models"
      contains: "R²"
  key_links:
    - from: "results/4ch_vs_7ch_r2_gap_report.md"
      to: "models/muse_4ch/eegnet_4ch_results.json"
      via: "data cited in report"
      pattern: "0\\.016"
    - from: "results/4ch_vs_7ch_r2_gap_report.md"
      to: "models/muse_4ch/summary_multiscale_tcn_4ch_lb20_hz5_ts1.json"
      via: "data cited in report"
      pattern: "0\\.156"
---

<objective>
Write results/4ch_vs_7ch_r2_gap_report.md — a formal report documenting the performance gap between 4-channel (Muse 2 proxy) and 7-channel (research-grade) models, using existing artifacts in models/muse_4ch/.

Purpose: RSRCH-05 requires a formal R² gap report. models/muse_4ch/ already contains the trained checkpoints, result JSONs, and an informal COMPARISON_REPORT.md. This plan produces the formal report in results/ with correct numbers, proper baseline comparisons, and the caveats a CSEF judge or reviewer needs.

Output: results/4ch_vs_7ch_r2_gap_report.md
</objective>

<execution_context>
@/Users/amaarchughtai/.claude/get-shit-done/workflows/execute-plan.md
@/Users/amaarchughtai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-scope-lock-foundation/HARDWARE_MODE_DECISION.md

Numbers to cite in the report (read directly from existing artifacts — do NOT recompute):

From models/muse_4ch/eegnet_4ch_results.json:
  - n_channels: 4, channels: [F7, F8, T7, T8]
  - n_params: 1409
  - test_r2_raw: 0.016303594744873418 → round to 0.016
  - test_rmse_raw: 3.97e-05

From models/muse_4ch/summary_multiscale_tcn_4ch_lb20_hz5_ts1.json:
  - model: MultiscaleCausalTCN, n_params: 29507
  - best_val_future_r2: 0.4467615485191345 → round to 0.447
  - test_future_metrics.r2: 0.15580248832702637 → round to 0.156
  - test_future_metrics.rmse: 3.76e-05
  - config.horizon: 5, config.lookback: 20

7ch baselines (from paper / existing results — use known values):
  - EEGNet 7ch test R²: 0.287 (established ceiling, cited in CLAUDE.md and multiple results)
  - TCN 7ch val R² at hz=5: from results/comparison_table_7ch.json where model="tcn" and horizon=5
  - TCN 7ch test R² at hz=5: same source

From results/comparison_table_7ch.json (horizon=5 rows):
  - persistence test_r2 at horizon 5 (need to read)
  - ridge test_r2 at horizon 5 (need to read)
  - tcn test_r2 at horizon 5 (need to read)

From results/comparison_table_4ch.json (horizon=5 rows):
  - persistence test_r2 at horizon 5
  - ridge test_r2 at horizon 5
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write 4ch vs 7ch R² gap report</name>
  <files>results/4ch_vs_7ch_r2_gap_report.md</files>
  <action>
Read the following files to extract exact numbers before writing:
- models/muse_4ch/eegnet_4ch_results.json
- models/muse_4ch/summary_multiscale_tcn_4ch_lb20_hz5_ts1.json
- results/comparison_table_7ch.json (filter to horizon=5 rows for persistence, ridge, tcn)
- results/comparison_table_4ch.json (filter to horizon=5 rows for persistence, ridge)

Then write results/4ch_vs_7ch_r2_gap_report.md with the following structure:

---
# 4-Channel vs 7-Channel Model Performance Gap

**Report date:** 2026-03-21
**Purpose:** Documents the performance cost of adapting from 7-channel research-grade EEG to the 4-channel Muse 2 proxy electrode configuration. Satisfies RSRCH-05.

## Hardware Configuration

| Configuration | Channels | Electrode Set | Hardware |
|---|---|---|---|
| Research-grade (7ch) | 7 | Fp1, Fp2, F3, F4, F7, F8, Fz | Gel cap, lab EEG |
| Muse 2 proxy (4ch) | 4 | F7, F8, T7, T8 | Dry electrode consumer headset |

Channel mapping: AF7→F7, AF8→F8, TP9→T7, TP10→T8 (nearest research-grade equivalents)

## 1. Static PAC Prediction (EEGNet)

| Configuration | Test R² | Test RMSE | Parameters |
|---|---|---|---|
| 7-channel (research-grade) | 0.287 | — | 1,457 |
| 4-channel (Muse 2 proxy) | 0.016 | 3.97e-05 | 1,409 |
| **Delta** | **-0.271** | — | -48 |

**EEGNet interpretation:** Static PAC prediction drops sharply from R²=0.287 to R²=0.016 when restricted to 4 channels. The 4-channel model is near chance — frontal PAC prediction relies heavily on spatial coverage that the 4-channel subset cannot provide.

## 2. Temporal PAC Forecasting (MultiscaleCausalTCN, horizon=5s)

Show a table with columns: Configuration | Persistence R² | Ridge R² | TCN R² | TCN vs Persistence Gain
Fill rows for 7ch and 4ch, using exact values from comparison_table_7ch.json and comparison_table_4ch.json (horizon=5 rows).
For TCN 4ch test R²: use 0.156 from summary_multiscale_tcn_4ch_lb20_hz5_ts1.json.
For TCN 7ch test R²: use the value from comparison_table_7ch.json where model contains "tcn" and horizon==5.

**Temporal forecasting interpretation:** The TCN maintains its advantage over persistence at both channel configurations at horizon=5s. The absolute R² drops between configurations, but the relative advantage (TCN vs persistence) quantifies the model's contribution independent of the hardware.

## 3. Why the Static Gap Is Larger Than the Temporal Gap

Explain: EEGNet predicts instantaneous PAC from raw EEG spectral features — its performance is highly sensitive to spatial sampling. The TCN predicts future PAC from a 20-second temporal history of spectral features; temporal context partially compensates for reduced spatial coverage because PAC trajectory patterns (stim_state, timing) carry predictive information that survives channel reduction.

## 4. Caveats

1. **PAC labels are not directly comparable.** 7ch PAC is the modulation index computed over 7 frontal channels; 4ch PAC is computed over 4 channels (F7/F8/T7/T8 only). The target distributions differ, so R² values should not be compared as absolute numbers — compare each against its own persistence baseline.

2. **This is a proxy comparison, not a true Muse 2 evaluation.** At deployment, the Muse 2 hardware adds: dry-electrode noise (impedance >300 kΩ), EMG contamination at frontal sites (30-40 Hz — overlaps gamma), reference mismatch. The actual Muse 2 deployment gap will be larger than what this proxy study shows.

3. **4ch TCN val R²=0.447 vs test R²=0.156 gap.** The val/test split difference is large, suggesting the 4ch TCN overfits to specific subjects in the val split. Report the test R² (0.156) as the honest estimate.

## 5. CSEF Defense Narrative

Write a 3-sentence paragraph: "The 7-channel research-grade model achieves R²=0.287 for static PAC prediction and TCN test R²=[7ch_tcn_r2] for 5-second forecasting. Adapting to 4-channel Muse 2 proxy positions reduces static prediction to R²=0.016, but the temporal forecasting TCN retains R²=0.156 — a [ratio]% retention of the research-grade performance. This quantifies the intelligence-vs-hardware tradeoff: temporal context from a 20-second history compensates for spatial information loss in the predictive forecasting task."

## 6. Source Files

List the artifact files this report was generated from (the JSON files in models/muse_4ch/).

---

Write clean Markdown. Round all R² values to 3 decimal places. Do not editorialize beyond what the numbers support.
  </action>
  <verify>
    <automated>python3 -c "
import re
content = open('/Users/amaarchughtai/Developer/research/closedloop-40hz-entrainment/results/4ch_vs_7ch_r2_gap_report.md').read()
assert '0.016' in content, 'missing EEGNet 4ch R2'
assert '0.287' in content, 'missing EEGNet 7ch R2'
assert '0.156' in content, 'missing TCN 4ch test R2'
assert 'RSRCH-05' in content, 'missing requirement reference'
assert 'Caveat' in content or 'caveat' in content, 'missing caveats section'
print('[PASS] 4ch_vs_7ch_r2_gap_report.md has required content')
"</automated>
  </verify>
  <done>results/4ch_vs_7ch_r2_gap_report.md exists with EEGNet comparison table (0.016 vs 0.287), TCN comparison table with persistence and Ridge baselines at horizon=5, caveats about non-comparable PAC labels, and CSEF defense narrative.</done>
</task>

</tasks>

<verification>
python3 -c "
from pathlib import Path
p = Path('results/4ch_vs_7ch_r2_gap_report.md')
assert p.exists(), 'report missing'
content = p.read_text()
for required in ['0.016', '0.287', '0.156', 'horizon', 'Caveat', 'proxy']:
    assert required in content, f'missing: {required}'
print('[PASS] all required content present')
"
</verification>

<success_criteria>
- results/4ch_vs_7ch_r2_gap_report.md exists with correct numbers from existing JSON artifacts
- Report includes EEGNet comparison table, TCN comparison table with baselines, caveats, and defense narrative
- RSRCH-05 requirement checkbox can be marked complete
</success_criteria>

<output>
After completion, create .planning/phases/14-scope-lock-completion/14-02-SUMMARY.md
</output>
