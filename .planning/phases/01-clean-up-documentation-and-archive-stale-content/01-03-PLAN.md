---
phase: 01-clean-up-documentation-and-archive-stale-content
plan: 03
type: execute
wave: 2
depends_on: ["01-01", "01-02"]
files_modified:
  - docs/INDEX.md
  - AGENTS.md
  - CLAUDE.md
autonomous: true
requirements: [CLEAN-05, CLEAN-06]

must_haves:
  truths:
    - "docs/INDEX.md accurately reflects the post-cleanup directory structure"
    - "No broken links in docs/INDEX.md"
    - "AGENTS.md redundancy with CLAUDE.md is eliminated"
    - "A single authoritative instruction file exists for coding agents"
    - "All AGENTS.md sections (Code Style, Typing, Naming, Error Handling, ML Guardrails, Data Hygiene, When Adding New Code, Commit Expectations) are present in CLAUDE.md"
  artifacts:
    - path: "docs/INDEX.md"
      provides: "Updated documentation index matching cleaned-up file layout"
      min_lines: 40
    - path: "CLAUDE.md"
      provides: "Single authoritative agent instruction file with ALL AGENTS.md sections merged"
      min_lines: 200
  key_links:
    - from: "docs/INDEX.md"
      to: "docs/"
      via: "Relative links to all active documentation files"
      pattern: "Every link target exists on disk"
---

<objective>
Update docs/INDEX.md to reflect the post-cleanup directory structure, and consolidate AGENTS.md into CLAUDE.md.

Purpose: After plans 01 and 02 remove/move files, the docs/INDEX.md directory structure diagram, file references, and quick navigation table will have broken links. AGENTS.md overlaps significantly with CLAUDE.md and adds confusion about which file is authoritative. All unique AGENTS.md content must be merged before removal.

Output: Accurate INDEX.md with no broken links, single CLAUDE.md as the authoritative coding agent instruction file with all AGENTS.md guidance preserved.
</objective>

<execution_context>
@/Users/amaarchughtai/.claude/get-shit-done/workflows/execute-plan.md
@/Users/amaarchughtai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@AGENTS.md
@docs/INDEX.md
@.planning/phases/01-clean-up-documentation-and-archive-stale-content/01-01-PLAN.md
@.planning/phases/01-clean-up-documentation-and-archive-stale-content/01-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update docs/INDEX.md for post-cleanup structure</name>
  <files>docs/INDEX.md</files>
  <action>
    Rewrite docs/INDEX.md to reflect the cleaned-up directory structure from plans 01 and 02. Specific changes:

    1. **Update "Last updated" date** to 2026-03-19.

    2. **Quick Navigation table** — Remove entries pointing to deleted locations:
       - Remove "Historical analysis reports" link to `reports/` (moved to `archive/reports/`)
       - Update "Comprehensive project map" link from `COMPREHENSIVE_PROJECT_MAP.md` (root of docs/) to `reference/COMPREHENSIVE_PROJECT_MAP.md`

    3. **Directory Structure diagram** — Update to reflect:
       - Remove `reports/` directory (moved to archive/reports/)
       - Remove COMPREHENSIVE_PROJECT_MAP.md and ML_ZERO_TO_HERO.md from docs/ root (moved to reference/)
       - Add COMPREHENSIVE_PROJECT_MAP.md and ML_ZERO_TO_HERO.md under reference/
       - Update archive/ section to show reports/ subdirectory, STATUS_REPORT files, AND the 3 historical .txt variant files (04_Research_Methodology_Proposed_Approach.txt, AD_40Hz_Entrainment_Research_Paper_IEEE.txt, Comprehensive_Methodology_Closed_Loop_40Hz_Entrainment.txt)
       - Remove generate_excalidraw.py and COMPREHENSIVE_PROJECT_MAP.excalidraw from diagram (these are in docs/ root but aren't documentation — they're code artifacts)
       - Show notebook/ pointing to notebooks/ directory (not a subdirectory of docs/)

    4. **Current (Active) Documents section** — Keep CURRENT_METHODOLOGY.md, CODE_MAP.md, FINDINGS.md. Add COMPREHENSIVE_PROJECT_MAP.md at new reference/ location.

    5. **Technical Reports section** — Move to an "Archived Reports" section noting they are in docs/archive/reports/ and are superseded.

    6. **Research Background section** — Remove references to .docx files (only .txt remain).

    7. **Archive section** — Update to reflect actual contents: 3 historical .txt variant files, STATUS_REPORT files, and reports/ subdirectory.

    8. **Key Discrepancies table** — Keep as-is (still accurate).

    9. **Verify all links** — After rewriting, check every relative link target exists. Use `test -f` for each linked file.
  </action>
  <verify>
    <automated>python3 -c "
import re, os
content = open('docs/INDEX.md').read()
links = re.findall(r'\[.*?\]\(((?!http)[^)]+)\)', content)
broken = []
for link in links:
    # Resolve relative to docs/
    if link.startswith('../'):
        path = os.path.normpath(os.path.join('.', link[3:]))
    else:
        path = os.path.normpath(os.path.join('docs', link))
    # Strip any anchor
    path = path.split('#')[0]
    if path and not os.path.exists(path):
        broken.append(path)
if broken:
    print(f'BROKEN LINKS: {broken}')
    exit(1)
print('All links valid - PASS')
"</automated>
  </verify>
  <done>docs/INDEX.md accurately reflects post-cleanup directory structure. All relative links resolve to existing files. No references to deleted/moved content.</done>
</task>

<task type="auto">
  <name>Task 2: Merge ALL AGENTS.md sections into CLAUDE.md and remove AGENTS.md</name>
  <files>AGENTS.md, CLAUDE.md</files>
  <action>
    AGENTS.md (166 lines) contains 7+ substantial sections that are either absent from or only partially covered in CLAUDE.md. Read both files fully, then merge ALL unique AGENTS.md content into CLAUDE.md. Here is a complete inventory of what must be merged:

    **Sections already in CLAUDE.md (skip or update only if AGENTS.md has unique details):**
    - Environment setup — identical, skip
    - Project layout — CLAUDE.md has more detail in Architecture Overview, but AGENTS.md has unique notes (see below)

    **Sections MISSING from CLAUDE.md that MUST be added from AGENTS.md:**

    1. **Code Style** (AGENTS.md lines 102-107) — Add as new "## Code Style" section in CLAUDE.md. Content: PEP 8, 4-space indent, readable line lengths under 100/120, prefer simple NumPy/PyTorch over abstraction-heavy frameworks, keep functions focused, match surrounding file.

    2. **Imports** (AGENTS.md lines 109-114) — Add as new "## Imports" section. Content: import order (stdlib, third-party, project-local), standard aliases (np, pd, plt, sns), sys.path.insert pattern in legacy src/, Path-based root discovery in newer modules, local imports only for heavy optional/circular deps.

    3. **Typing** (AGENTS.md lines 116-120) — Add as new "## Typing" section. Content: type hints on public functions/methods/helpers, in src/ use typing imports (Dict, List, Optional, Tuple), in newer modules use `from __future__ import annotations` + built-in generics, use hints to clarify shapes not add noise.

    4. **Naming and Docstrings** (AGENTS.md lines 122-128) — Add as new "## Naming and Docstrings" section. Content: snake_case for modules/functions/variables/CLI flags, PascalCase for classes, UPPER_CASE for constants, preserve abbreviations (pac, eeg, snr, fs, r2), descriptive checkpoint names, module docstrings, prefer good names/docstrings over inline comments.

    5. **Error Handling and Logging** (AGENTS.md lines 130-139) — Add as new "## Error Handling and Logging" section. Content: assert for invariants/shape checks, ValueError for invalid inputs, FileNotFoundError for missing files, audit scripts return bool and SystemExit(1), epsilon guards, logging.getLogger(__name__) or closed_loop_entrainment logger, print() ok for one-shot scripts, [PASS]/[FAIL] style for audits, argparse for CLIs.

    6. **ML Guardrails** (AGENTS.md lines 141-147) — Add as new "## ML Guardrails" section. Content: no subject overlap across splits, causal indexing (future targets strictly after sequence end), fit normalization on train only, self-describing checkpoints, deterministic seeding, report persistence and Ridge baselines.

    7. **Data and Artifact Hygiene** (AGENTS.md lines 149-153) — Add as new "## Data and Artifact Hygiene" section. Content: never commit raw/private data/credentials/large artifacts, write checkpoints to models/, reports to results/, logs to logs/, don't mix outputs into src/ or temporal/, update docs when changing defaults/interfaces.

    8. **When Adding New Code** (AGENTS.md lines 155-159) — Add as new "## When Adding New Code" section. Content: put active implementation in src/, temporal_multiscale/, rigor/, or scripts/, add self-test or audit hook, reuse existing utilities, optimize for correctness/leakage-safety/reproducibility.

    **Unique items to merge into EXISTING sections:**

    9. Under "Architecture Overview" or a new "### Additional Directories" subsection:
       - `rigor/`: Extended robustness, multi-seed, and statistical validation scripts.
       - `scripts/pipeline/`: Orchestration scripts; some entry points have fragile import-path assumptions — use carefully.

    10. Under "Critical Gotchas" add:
        - "Do not extend `archive/` unless explicitly asked."

    11. **Testing Strategy** (AGENTS.md lines 59-63) — The "Running A Single Test" section (lines 65-82) has unique content: self-test pattern for src/ modules (`python src/eegnet.py`, `python src/controller.py`, etc.) and `python -c` examples for running specific audit functions. Merge into "### Testing" section of CLAUDE.md.

    12. **Lightweight Lint And Syntax Checks** (AGENTS.md lines 84-93) — Add as "## Lint and Syntax Checks" section. Content: no canonical formatter/linter configured, default to syntax checks, py_compile and compileall commands, if using ruff/black/mypy keep scope narrow.

    13. **Validation Matrix** (AGENTS.md lines 95-100) — Add as "## Validation Matrix" section. Content: per-module validation commands (eegnet, controller, preprocessing/PAC, temporal, split logic).

    **Commit Expectations** (AGENTS.md lines 161-165) — Overlaps with existing "## Commit Style" section. Merge the unique items: "include before/after metrics such as R^2, RMSE, alignment, or stimulation efficiency" and "Record the exact validation commands you ran" — both are already in CLAUDE.md. Skip if fully covered.

    After merging all sections, run `git rm AGENTS.md`.

    Do NOT change any existing CLAUDE.md content — only append sections. Place new sections after "## Commit Style" at the end, in this order: Code Style, Imports, Typing, Naming and Docstrings, Error Handling and Logging, ML Guardrails, Data and Artifact Hygiene, When Adding New Code, Lint and Syntax Checks, Validation Matrix.
  </action>
  <verify>
    <automated>test ! -f AGENTS.md && grep -q "snake_case" CLAUDE.md && grep -q "ML Guardrails" CLAUDE.md && grep -q "Data and Artifact Hygiene" CLAUDE.md && grep -q "Error Handling" CLAUDE.md && grep -q "When Adding New Code" CLAUDE.md && grep -q "Lint and Syntax" CLAUDE.md && grep -q "Validation Matrix" CLAUDE.md && test $(wc -l < CLAUDE.md | tr -d ' ') -gt 200 && echo "PASS"</automated>
  </verify>
  <done>AGENTS.md removed. All 10+ sections merged into CLAUDE.md: Code Style, Imports, Typing, Naming and Docstrings, Error Handling and Logging, ML Guardrails, Data and Artifact Hygiene, When Adding New Code, Lint and Syntax Checks, Validation Matrix, plus unique items merged into existing sections. CLAUDE.md exceeds 200 lines (was 158). Single authoritative instruction file for all coding agents. No information lost.</done>
</task>

</tasks>

<verification>
- docs/INDEX.md link validation script passes (no broken links)
- AGENTS.md does not exist
- CLAUDE.md contains all unique AGENTS.md sections (grep confirms: snake_case, ML Guardrails, Data and Artifact Hygiene, Error Handling, When Adding New Code, Lint and Syntax, Validation Matrix)
- CLAUDE.md line count > 200 (confirms substantial content was added, not just trivial edits)
- `git status` shows clean state
</verification>

<success_criteria>
- docs/INDEX.md accurately maps the cleaned-up docs/ directory
- Zero broken links in docs/INDEX.md
- AGENTS.md eliminated, CLAUDE.md is the single source of truth
- No information lost — ALL AGENTS.md sections preserved in CLAUDE.md (verified by unique string checks and line count assertion)
</success_criteria>

<output>
After completion, create `.planning/phases/01-clean-up-documentation-and-archive-stale-content/01-03-SUMMARY.md`
</output>
