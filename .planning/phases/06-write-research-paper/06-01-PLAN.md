---
phase: 06-write-research-paper
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/generate_paper_figures.py
  - results/figures/horizon_sweep.png
  - results/figures/horizon_sweep.pdf
  - results/figures/system_block_diagram.png
  - results/figures/system_block_diagram.pdf
  - docs/paper/sections/.gitkeep
autonomous: true
requirements:
  - PAPER-FIG
must_haves:
  truths:
    - "Horizon sweep figure exists showing TCN vs Persistence vs Ridge across 1-10s horizons"
    - "System block diagram exists showing the full EEG-to-stimulation pipeline"
    - "docs/paper/sections/ directory exists for section-based paper writing"
  artifacts:
    - path: "scripts/generate_paper_figures.py"
      provides: "Figure generation script for both new figures"
      min_lines: 80
    - path: "results/figures/horizon_sweep.png"
      provides: "Horizon sweep line graph (PNG)"
    - path: "results/figures/horizon_sweep.pdf"
      provides: "Horizon sweep line graph (PDF)"
    - path: "results/figures/system_block_diagram.png"
      provides: "System architecture block diagram (PNG)"
    - path: "results/figures/system_block_diagram.pdf"
      provides: "System architecture block diagram (PDF)"
  key_links:
    - from: "scripts/generate_paper_figures.py"
      to: "models/sweep_horizons_results.json"
      via: "json.load for exact data points"
      pattern: "sweep_horizons_results"
---

<objective>
Generate the two new publication-quality figures required for the research paper and set up the paper directory structure.

Purpose: The paper needs two figures that do not yet exist: (1) a prediction horizon sweep line graph showing the TCN's advantage over baselines at 5-10s horizons, and (2) a system architecture block diagram showing the full closed-loop pipeline. Both are critical for the paper's central argument. The paper directory structure is also created here so parallel plans can write sections independently.

Output: `scripts/generate_paper_figures.py`, two new figures in `results/figures/` (PNG+PDF), `docs/paper/sections/` directory.
</objective>

<execution_context>
@/Users/amaarchughtai/.claude/get-shit-done/workflows/execute-plan.md
@/Users/amaarchughtai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-write-research-paper/06-CONTEXT.md
@.planning/phases/06-write-research-paper/06-RESEARCH.md

@models/sweep_horizons_results.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create figure generation script and produce both new figures</name>
  <files>scripts/generate_paper_figures.py, results/figures/horizon_sweep.png, results/figures/horizon_sweep.pdf, results/figures/system_block_diagram.png, results/figures/system_block_diagram.pdf</files>
  <action>
Create `scripts/generate_paper_figures.py` with two functions:

**Function 1: `generate_horizon_sweep()`**
- Read exact data from `models/sweep_horizons_results.json`
- Line graph with horizons [1, 2, 3, 5, 8, 10] on x-axis, R-squared on y-axis
- Three lines: Persistence baseline (circle markers, blue #2196F3), Ridge regression (square markers, orange #FF9800), Causal TCN (triangle markers, green #4CAF50, thicker line width=2.5)
- Horizontal dashed black line at R-squared = 0
- Light green shaded region from x=4.5 to x=10.5 labeled "Operationally useful range (5-10s)"
- X-axis label: "Prediction Horizon (seconds)", Y-axis: "R-squared"
- Title: "PAC Forecasting Performance vs. Prediction Horizon"
- Legend with all three models + shaded region
- Figure size (8, 5), clean publication style (use matplotlib defaults with minor grid)
- Save as both PNG (300 dpi) and PDF to results/figures/

**Important caption note embedded as comment in the script:** "Evaluated with 5-window causal target smoothing (ts=5). Deployed checkpoint uses raw targets (ts=1); see Supplementary Table S1 for absolute performance under that condition."

**Function 2: `generate_system_block_diagram()`**
- Use matplotlib patches (FancyBboxPatch, FancyArrowPatch) to create a clean block diagram
- Flow: Patient EEG (7 frontal channels, 250 Hz) -> Preprocessing (BP 0.5-80 Hz, notch, CAR) -> EEGNet (1,457 params) -> PAC Estimate -> Feature Extraction (73 features: 61 spectral + 7 PAC-derived + 5 stim context) -> MultiscaleCausalTCN (31K params, 20s lookback) -> Predicted PAC (5s ahead) -> Closed-Loop Controller (z-score thresholds +/-0.5, 5s hysteresis) -> STIMULATE/REST/MAINTAIN -> 40 Hz Audio -> feedback arrow back to Patient
- Use a horizontal left-to-right flow with a feedback loop arrow curving back from output to input
- Color coding: blue for data/signal blocks, green for ML model blocks, orange for decision/controller blocks
- Annotations on arrows with key specs (e.g., "2s windows", "<50ms inference")
- Figure size (14, 6), clean publication style
- Save as both PNG (300 dpi) and PDF to results/figures/

**Main block:** When run as script, generate both figures, print confirmation with file paths.

Do NOT use SciencePlots or any non-standard dependency -- stick to matplotlib only (already installed).
  </action>
  <verify>
    <automated>cd /Users/amaarchughtai/Developer/research/closedloop-40hz-entrainment && source venv/bin/activate && python scripts/generate_paper_figures.py && ls -la results/figures/horizon_sweep.png results/figures/horizon_sweep.pdf results/figures/system_block_diagram.png results/figures/system_block_diagram.pdf</automated>
  </verify>
  <done>Both figures exist as PNG+PDF in results/figures/. Horizon sweep shows 6 data points per line with clear TCN advantage at 5-10s. Block diagram shows complete EEG-to-stimulation pipeline with feedback loop.</done>
</task>

<task type="auto">
  <name>Task 2: Create paper directory structure</name>
  <files>docs/paper/sections/.gitkeep</files>
  <action>
Create the `docs/paper/sections/` directory with a `.gitkeep` file. This directory will hold individual section markdown files that parallel plans write into. The sections will be:

- 01-abstract.md
- 02-introduction.md
- 03-literature-review.md
- 04-methods.md
- 05-architecture-search.md
- 06-results.md
- 07-discussion.md
- 08-future-directions.md
- 09-conclusion.md
- 10-data-availability.md
- 11-references.md

Do NOT create these files yet (other plans will create them). Just create the directory with .gitkeep so the path exists.
  </action>
  <verify>
    <automated>ls -la /Users/amaarchughtai/Developer/research/closedloop-40hz-entrainment/docs/paper/sections/.gitkeep</automated>
  </verify>
  <done>docs/paper/sections/ directory exists and is ready for section files from Plans 02-05.</done>
</task>

</tasks>

<verification>
- `python scripts/generate_paper_figures.py` runs without errors and produces 4 files (2 PNG, 2 PDF)
- Horizon sweep figure shows R-squared values matching models/sweep_horizons_results.json exactly
- Block diagram shows complete pipeline from EEG input to stimulation output with feedback loop
- docs/paper/sections/ directory exists
</verification>

<success_criteria>
Two new publication-quality figures generated and saved. Paper directory structure created. All downstream plans can reference figures and write section files.
</success_criteria>

<output>
After completion, create `.planning/phases/06-write-research-paper/06-01-SUMMARY.md`
</output>
