---
phase: 08-fix-internal-consistency
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/paper/RESEARCH_PAPER.md
autonomous: true
requirements:
  - CONS-01
  - CONS-04
  - CONS-05
  - CONS-06
  - CONS-07

must_haves:
  truths:
    - "Every mention of the subject population uses '35 elderly subjects', never '35 dementia patients' or '35 elderly dementia patients'"
    - "Section 2.2 heading reads '2.2 40 Hz Sensory Entrainment' (space between '2.2' and '40')"
    - "Every first-person pronoun in the paper body is 'I' or 'my' — no 'we' or 'our' appears outside quoted text"
    - "'Reactive thresholding' at L80 is replaced with 'Reactive Threshold'"
    - "Orders-of-magnitude language is consistently 'nearly three orders of magnitude' wherever EEGNet parameter count is described relative to the transformer"
  artifacts:
    - path: "docs/paper/RESEARCH_PAPER.md"
      provides: "Corrected research paper with all text consistency fixes"
      contains: "35 elderly subjects"
  key_links:
    - from: "L79 Contribution 3"
      to: "correct population label"
      via: "text replacement"
      pattern: "35 elderly subjects"
    - from: "L732 Conclusion"
      to: "correct population label"
      via: "text replacement"
      pattern: "35 elderly subjects"
    - from: "L113 Section 2.2 heading"
      to: "correctly spaced heading"
      via: "text replacement"
      pattern: "### 2.2 40 Hz"
---

<objective>
Apply five targeted text corrections to docs/paper/RESEARCH_PAPER.md that fix population label wording, section heading formatting, first-person voice, a terminology inconsistency, and an orders-of-magnitude inconsistency. None of these changes affect the reference numbering.

Purpose: Establish a consistent, single-author voice and accurate descriptive language throughout the paper before the reference renumbering pass in Plan 02.
Output: docs/paper/RESEARCH_PAPER.md with CONS-01, CONS-04, CONS-05, CONS-06, CONS-07 resolved.
</objective>

<execution_context>
@/Users/amaarchughtai/.claude/get-shit-done/workflows/execute-plan.md
@/Users/amaarchughtai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/paper/RESEARCH_PAPER.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix population label, section heading, terminology, and magnitude phrase</name>
  <files>docs/paper/RESEARCH_PAPER.md</files>
  <action>
    Make four targeted text edits to docs/paper/RESEARCH_PAPER.md. Read the full file first, then apply each fix:

    **CONS-01 — Population label (2 locations):**

    Location 1 (~L79, Contribution 3):
    - Find: "35 real dementia patient EEGs" (in the Contribution 3 bullet)
    - Replace with: "35 elderly subjects' EEG recordings"
    - Context: "Contribution 3: Closed-loop controller validated on..."

    Location 2 (~L732, Conclusion first paragraph):
    - Find: "35 elderly dementia patients"
    - Replace with: "35 elderly subjects"
    - Context: "The system was trained and evaluated on real EEG recordings from..."

    **CONS-04 — Section heading formatting (1 location):**

    Location (~L113):
    - Find: `### 2.240 Hz Sensory Entrainment: Mechanisms and Clinical Evidence`
    - Replace with: `### 2.2 40 Hz Sensory Entrainment: Mechanisms and Clinical Evidence`
    - Note: The missing space between "2.2" and "40" causes the heading to render as "2.240 Hz".

    **CONS-06 — Terminology (1 location):**

    Location (~L80, Contribution 3):
    - Find: "reactive thresholding" (lowercase 't', after the population label fix above)
    - Replace with: "Reactive Threshold"
    - Context: "72.1% alignment with patient therapeutic need versus 64.5% for reactive thresholding"

    **CONS-07 — Orders of magnitude (1 location):**

    Location (~L436, Section 4.3):
    - Find: "spanning four orders of magnitude in parameter count"
    - Replace with: "spanning nearly three orders of magnitude in parameter count"
    - Rationale: 1,457 to 1.1M parameters = log10(1,100,000/1,457) ≈ 2.88 = "nearly three orders of magnitude". L74 already correctly says "nearly three orders of magnitude" — make L436 match.

    Do NOT change L74's "nearly three orders of magnitude" — it is already correct.
  </action>
  <verify>
    <automated>
      grep -n "dementia patient" /Users/amaarchughtai/Developer/research/closedloop-40hz-entrainment/docs/paper/RESEARCH_PAPER.md | grep -v "dementia patients undergo\|dementia patients who\|dementia patients\." | wc -l
      # Must return 0 (no remaining wrong population label)
      grep -n "2\.240 Hz" /Users/amaarchughtai/Developer/research/closedloop-40hz-entrainment/docs/paper/RESEARCH_PAPER.md | wc -l
      # Must return 0 (heading fixed)
      grep -n "reactive thresholding" /Users/amaarchughtai/Developer/research/closedloop-40hz-entrainment/docs/paper/RESEARCH_PAPER.md | wc -l
      # Must return 0
      grep -n "four orders of magnitude" /Users/amaarchughtai/Developer/research/closedloop-40hz-entrainment/docs/paper/RESEARCH_PAPER.md | wc -l
      # Must return 0
    </automated>
  </verify>
  <done>
    - "35 elderly subjects" appears at both the Contribution 3 and Conclusion locations
    - Section 2.2 heading has a space: "### 2.2 40 Hz Sensory Entrainment..."
    - "reactive thresholding" replaced with "Reactive Threshold" at Contribution 3
    - "four orders of magnitude" replaced with "nearly three orders of magnitude" at Section 4.3
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace all "we/our" with "I/my" throughout body (CONS-05)</name>
  <files>docs/paper/RESEARCH_PAPER.md</files>
  <action>
    Read docs/paper/RESEARCH_PAPER.md. The abstract already uses "I" correctly. The paper body has approximately 13 occurrences of "we" or "our" that must become "I" or "my". Apply each replacement:

    Work through the file from top to bottom. For each occurrence of "we" or "our" in the paper body (everything after the Abstract section), apply the grammatically correct singular form:

    - "We approach" → "I approach"
    - "We used" → "I used"
    - "We applied" → "I applied"
    - "We selected" → "I selected"
    - "We computed" → "I computed"
    - "we computed" → "I computed"
    - "we adapted" → "I adapted"
    - "we constructed" → "I constructed"
    - "we trained" → "I trained"
    - "we conducted" → "I conducted"
    - "our leakage audit" → "my leakage audit"
    - "we should ask" → "I should ask"
    - "we evaluated" → "I evaluated"

    IMPORTANT: Do NOT change "we" inside quoted text from other authors, inside dataset/paper titles, or inside clearly third-person citations that describe other researchers' work ("Murdock et al. showed that we..."). Only change first-person self-referential uses.

    After replacements, verify the Abstract section is unchanged (still uses "I" correctly).
  </action>
  <verify>
    <automated>
      grep -n "\bwe\b\|\bour\b\|\bWe\b\|\bOur\b" /Users/amaarchughtai/Developer/research/closedloop-40hz-entrainment/docs/paper/RESEARCH_PAPER.md
      # Any remaining lines must be inspected; target is 0 self-referential "we/our" occurrences
    </automated>
  </verify>
  <done>
    All self-referential "we/our" in the paper body changed to "I/my". Running the grep returns zero body-section matches or only legitimate non-self-referential uses. The Abstract section is unchanged.
  </done>
</task>

</tasks>

<verification>
After both tasks:
1. `grep -n "35 elderly dementia\|dementia patient EEGs" docs/paper/RESEARCH_PAPER.md` returns 0 matches
2. `grep -n "2\.240 Hz" docs/paper/RESEARCH_PAPER.md` returns 0 matches
3. `grep -n "reactive thresholding" docs/paper/RESEARCH_PAPER.md` returns 0 matches
4. `grep -n "four orders of magnitude" docs/paper/RESEARCH_PAPER.md` returns 0 matches
5. `grep -n "\bwe\b\|\bour\b\|\bWe\b\|\bOur\b" docs/paper/RESEARCH_PAPER.md` returns 0 self-referential matches
</verification>

<success_criteria>
- CONS-01: Both incorrect population labels corrected to "35 elderly subjects"
- CONS-04: Section 2.2 heading correctly spaced
- CONS-05: All ~13 "we/our" occurrences in body replaced with "I/my"
- CONS-06: "reactive thresholding" standardized to "Reactive Threshold"
- CONS-07: "four orders of magnitude" replaced with "nearly three orders of magnitude" at Section 4.3
</success_criteria>

<output>
After completion, create `.planning/phases/08-fix-internal-consistency/08-01-SUMMARY.md` summarizing:
- Which lines were changed for each requirement
- Any edge cases encountered during the "we/our" replacement
- Confirmation grep output showing zero remaining violations
</output>
