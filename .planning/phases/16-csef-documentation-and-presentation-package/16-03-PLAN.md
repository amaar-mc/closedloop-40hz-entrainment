---
phase: 16-csef-documentation-and-presentation-package
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/generate_csef_presentation.py
  - docs/presentations/CSEF_2026_Presentation.pdf
  - CSEF/Presentation/CSEF_2026_Presentation.pdf
autonomous: true
requirements:
  - CSEF-PRESENTATION-PDF

must_haves:
  truths:
    - "Presentation PDF references 12 PAC+stim features, not 73"
    - "TCN R2 values in PDF are 0.606 (7ch) / 0.430 (4ch)"
    - "PDF includes PAC+Stim feature discovery as a key results section"
    - "PDF includes productization narrative (caregiver app, clinical roadmap)"
    - "PDF is 13 pages or fewer (CSEF requirement)"
    - "PDF is generated from the Python script (reproducible, not hand-edited)"
  artifacts:
    - path: "scripts/generate_csef_presentation.py"
      provides: "Updated fpdf2 presentation generator"
      contains: "12 features"
    - path: "docs/presentations/CSEF_2026_Presentation.pdf"
      provides: "Generated presentation PDF"
    - path: "CSEF/Presentation/CSEF_2026_Presentation.pdf"
      provides: "CSEF submission copy of presentation PDF"
  key_links:
    - from: "scripts/generate_csef_presentation.py"
      to: "docs/presentations/CSEF_2026_Presentation.pdf"
      via: "PDF generation"
      pattern: "fpdf"
    - from: "scripts/generate_csef_presentation.py"
      to: "experimental/FINDINGS.md"
      via: "Number traceability"
      pattern: "0\\.606|0\\.430|12 features"
---

<objective>
Update the CSEF presentation PDF generator script with PAC+Stim breakthrough content and regenerate the PDF.

Purpose: The presentation PDF is a core CSEF submission artifact (max 13 pages). The current PDF uses stale 73-feature model numbers. The generator is a Python script using fpdf2 that produces a landscape-format PDF.

Output: Updated generator script and regenerated PDF in both docs/presentations/ and CSEF/Presentation/.
</objective>

<execution_context>
@/Users/amaarchughtai/.claude/get-shit-done/workflows/execute-plan.md
@/Users/amaarchughtai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-csef-documentation-and-presentation-package/16-RESEARCH.md

Source documents:
@scripts/generate_csef_presentation.py
@experimental/FINDINGS.md
@results/RESULTS_REPORT.md
@results/4ch_vs_7ch_r2_gap_report.md

<interfaces>
<!-- The generator uses fpdf2 (FPDF class) with Times New Roman fonts -->
<!-- Output path: docs/presentations/CSEF_2026_Presentation.pdf -->
<!-- Format: landscape letter (11" x 8.5"), 12 pages, all-black text -->
<!-- Key sections to update: Methods (feature engineering), Results (TCN R2, horizon sweep), Discussion -->

<!-- Numbers to embed in generator (from experimental/FINDINGS.md): -->
Feature ablation headline: 73 features R2=-0.025 → 12 features R2=0.606 (5-seed mean)
Multi-seed: 0.606 +/- 0.032, range 0.558-0.647
4ch: R2=0.430 (vs 0.117 persistence)
Architecture search: h=32 high-reg R2=0.613 (5,154 params, best efficiency)
Horizon sweep (PAC+Stim): h=5 R2=0.577, h=10 R2=0.669 (7ch)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update generate_csef_presentation.py with PAC+Stim content</name>
  <files>scripts/generate_csef_presentation.py</files>
  <action>
Read scripts/generate_csef_presentation.py in full (817 lines). Understand the page structure, section boundaries, and where numbers are hardcoded in string literals.

Make these updates to the generator script:

1. **Methods section — Feature Engineering**: Replace the description of 73 features with the 12 PAC+stim features. Add the feature ablation discovery narrative: dropping spectral features raised R2 from 0.12 to 0.60. Explain WHY: spectral features encode subject-specific anatomy that doesn't generalize (val-test gap 0.358 to 0.246).

2. **Results section — TCN Performance**: Replace all R2 values:
   - "test R2 = 0.170" -> "test R2 = 0.606 (7ch, 5-seed mean 0.606 +/- 0.032)"
   - Add 4ch result: "test R2 = 0.430"
   - Update horizon sweep numbers to PAC+Stim values from horizon_sweep_pac_stim.json

3. **Results section — Feature Ablation Table**: Add a table (or update existing) showing the feature ablation results:
   | Features | Count | Test R2 |
   | All (spectral + PAC + stim) | 73 | -0.025 |
   | PAC + Stim context | 12 | 0.606 |
   | PAC only | 7 | 0.344 |
   | Spectral only | 61 | -0.420 |

4. **Discussion section**: Add the scientific interpretation — feature selection matters more than architecture. The 12 PAC+stim features capture causal temporal dynamics that transfer across subjects. Link to the 8-model architecture convergence (static R2 = 0.287 ceiling) as evidence that the bottleneck was always in the features, not the model.

5. **Productization / Broader Impact**: Add brief section on the caregiver app deployment (HF Spaces), clinical roadmap (3-phase: observational, feasibility, comparative), and the facility pilot preparation. This replaces any generic "future work" content.

6. **Multi-seed robustness**: Add to Results: "Validated across 5 random seeds: mean R2 = 0.606, std = 0.032, range 0.558-0.647."

7. **Page count management**: For every section added, identify what existing content it replaces or compresses. The architecture exploration history can be condensed (8 models all hit 0.287 — one sentence). The old TCN description with 73 features can be shortened. Target: stay at or under 13 pages. If the current script generates 12 pages, there is 1 page of headroom.

8. **Parameter count**: Update TCN param count from 31,043 to 5,154 (h=32 PAC+stim model) or 22,914 (h=64).

Do NOT change the PDF layout, font, or formatting infrastructure — only update content strings and tables within the existing page structure.

After updating the script, verify it compiles and has no stale numbers:
```bash
python3 -m py_compile scripts/generate_csef_presentation.py
grep -n "73 feat\|61 spectral\|R2 = 0.170\|test R2 = 0.170" scripts/generate_csef_presentation.py | grep -v "#.*previous\|#.*before\|#.*old\|#.*baseline\|#.*original"
```
The stale-number grep on the generator source is critical because the PDF is binary and cannot be audited by grep. Stale strings in the generator propagate silently into the final PDF.
  </action>
  <verify>
    <automated>cd /Users/amaarchughtai/Developer/research/closedloop-40hz-entrainment && python3 -m py_compile scripts/generate_csef_presentation.py && grep -c "12 features\|0.606\|PAC.*stim" scripts/generate_csef_presentation.py | xargs test 0 -lt && grep -n "73 feat\|61 spectral\|R2 = 0.170\|test R2 = 0.170" scripts/generate_csef_presentation.py | grep -v "#.*previous\|#.*before\|#.*old\|#.*baseline\|#.*original\|#.*was" | wc -l | xargs test 0 -eq</automated>
  </verify>
  <done>Generator script updated with all PAC+Stim numbers, feature ablation table, productization section. Script compiles without errors. Zero stale number references in generator source (outside commented historical context).</done>
</task>

<task type="auto">
  <name>Task 2: Generate presentation PDF, verify page count, and sync to CSEF/</name>
  <files>
    docs/presentations/CSEF_2026_Presentation.pdf
    CSEF/Presentation/CSEF_2026_Presentation.pdf
  </files>
  <action>
Run the updated generator script to produce the PDF:
```bash
cd /Users/amaarchughtai/Developer/research/closedloop-40hz-entrainment
python3 scripts/generate_csef_presentation.py
```

If the script fails, diagnose and fix the error (likely a missing fpdf2 dependency or font path issue on macOS). Install fpdf2 if needed: `pip install fpdf2`.

After successful generation:
1. Verify the PDF exists at the expected output path (docs/presentations/CSEF_2026_Presentation.pdf)
2. **Check page count — must be 13 or fewer (CSEF hard requirement).** Use pypdf or pdfinfo to count pages programmatically:
```bash
python3 -c "
from pypdf import PdfReader
reader = PdfReader('docs/presentations/CSEF_2026_Presentation.pdf')
n = len(reader.pages)
print(f'PDF page count: {n}')
assert n <= 13, f'CSEF VIOLATION: PDF has {n} pages (max 13)'
print('Page count OK')
"
```
If pypdf is not installed, install it (`pip install pypdf`) or fall back to:
```bash
python3 -c "
from fpdf import FPDF
import subprocess
result = subprocess.run(['mdls', '-name', 'kMDItemNumberOfPages', 'docs/presentations/CSEF_2026_Presentation.pdf'], capture_output=True, text=True)
print(result.stdout)
"
```
3. Copy to CSEF/Presentation/CSEF_2026_Presentation.pdf

The PDF is binary, so diff is not meaningful. Instead verify:
- File size is reasonable (the previous PDF was 371KB)
- File exists in both locations
- Both copies are identical size
- Page count is 13 or fewer
  </action>
  <verify>
    <automated>cd /Users/amaarchughtai/Developer/research/closedloop-40hz-entrainment && test -f docs/presentations/CSEF_2026_Presentation.pdf && test -f CSEF/Presentation/CSEF_2026_Presentation.pdf && python3 -c "
import os
from pypdf import PdfReader
s1 = os.path.getsize('docs/presentations/CSEF_2026_Presentation.pdf')
s2 = os.path.getsize('CSEF/Presentation/CSEF_2026_Presentation.pdf')
assert s1 > 10000, f'PDF too small: {s1}'
assert s1 == s2, f'Size mismatch: {s1} vs {s2}'
reader = PdfReader('docs/presentations/CSEF_2026_Presentation.pdf')
n = len(reader.pages)
assert n <= 13, f'CSEF VIOLATION: PDF has {n} pages (max 13)'
print(f'PDF OK: {s1} bytes, {n} pages, both copies match')
"</automated>
  </verify>
  <done>Presentation PDF generated, page count verified at 13 or fewer, verified non-trivial size, and synced to both docs/presentations/ and CSEF/Presentation/.</done>
</task>

</tasks>

<verification>
1. Generator script compiles: `python3 -m py_compile scripts/generate_csef_presentation.py`
2. Generator script has no stale numbers: `grep -n "73 feat\|61 spectral\|R2 = 0.170" scripts/generate_csef_presentation.py | grep -v "#.*previous\|#.*before\|#.*old\|#.*baseline\|#.*original"` — zero matches
3. PDF exists in both locations with matching file sizes
4. Page count check: `python3 -c "from pypdf import PdfReader; r = PdfReader('docs/presentations/CSEF_2026_Presentation.pdf'); assert len(r.pages) <= 13"`
5. New content present in generator: `grep -c "0.606\|12 features\|PAC.*stim\|feature ablation" scripts/generate_csef_presentation.py` — multiple matches
</verification>

<success_criteria>
- scripts/generate_csef_presentation.py updated with all PAC+Stim numbers
- Generator script has zero stale number references (outside historical context comments)
- PDF generated successfully at docs/presentations/CSEF_2026_Presentation.pdf
- PDF copied to CSEF/Presentation/CSEF_2026_Presentation.pdf (identical size)
- PDF is 13 pages or fewer (verified programmatically, not just by file size)
- No stale 73-feature or R2=0.170 references in generator (outside historical context)
</success_criteria>

<output>
After completion, create `.planning/phases/16-csef-documentation-and-presentation-package/16-03-SUMMARY.md`
</output>
