---
phase: 04-finalize-lab-notebook
plan: 03
type: execute
wave: 3
depends_on:
  - 04-01
  - 04-02
files_modified:
  - notebooks/P10_Research_Log_Notebook_Corrected_REVIEW.md
  - docs/notebook/README.md
  - docs/INDEX.md
autonomous: false
requirements:
  - FNL-04
  - FNL-05
  - FNL-06
user_setup: []
must_haves:
  truths:
    - "Reviewers can inspect chronology, fairness, and judge readability without touching the legacy notebook files."
    - "Repository navigation explains where the corrected notebook bundle lives and how manual PDF export works."
    - "Final signoff separates automated sanity checks from human editorial judgment."
  artifacts:
    - path: "notebooks/P10_Research_Log_Notebook_Corrected_REVIEW.md"
      provides: "Human review checklist, change log, and manual export instructions"
    - path: "docs/notebook/README.md"
      provides: "Minimal pointer to the notebook review bundle"
    - path: "docs/INDEX.md"
      provides: "Updated notebook navigation to the review-safe bundle"
  key_links:
    - from: "docs/INDEX.md"
      to: "docs/notebook/README.md"
      via: "notebook navigation row"
      pattern: "notebook/README\\.md"
    - from: "docs/notebook/README.md"
      to: "notebooks/generate_notebook_pdf.py"
      via: "manual export instructions"
      pattern: "generate_notebook_pdf\\.py"
---

<objective>
Package the corrected notebook for review, add the final checklist, and gate signoff with explicit human verification.

Purpose: Finish Phase 4 with a judge-ready review flow that documents provenance, fixes navigation confusion, and verifies that the FNL-04 chronology/no-hindsight rules were actually met before manual PDF export.
Output: A completed review sidecar, minimal docs pointer, index cleanup, and a blocking human review checkpoint.
</objective>

<execution_context>
@/Users/amaarchughtai/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/amaarchughtai/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-finalize-lab-notebook/04-CONTEXT.md
@.planning/phases/04-finalize-lab-notebook/04-RESEARCH.md
@.planning/phases/04-finalize-lab-notebook/04-VALIDATION.md
@docs/INDEX.md
@docs/reference/SYNOPSYS_REFERENCE.md
@docs/reference/feedback-kushal.md
@notebooks/P10_Research_Log_Notebook_Corrected.md
@notebooks/P10_Research_Log_Notebook_Corrected_EVIDENCE_MAP.md
@notebooks/generate_notebook_pdf.py

<interfaces>
From `docs/INDEX.md` today:

```markdown
| Lab notebook (formal) | [`notebook/LAB_NOTEBOOK.md`](notebook/LAB_NOTEBOOK.md) |
```

From `docs/reference/SYNOPSYS_REFERENCE.md`:

```markdown
- Must show: chronological entries with dates, raw data, problems encountered, iterations, references
- For digital notebooks: Do NOT delete errors -- strike through and annotate instead
```

The packaging pass should fix the notebook navigation minimally, not by broad docs reorganization.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Finalize the review sidecar and packaging pointer</name>
  <files>notebooks/P10_Research_Log_Notebook_Corrected_REVIEW.md, docs/notebook/README.md, docs/INDEX.md</files>
  <action>Populate the review sidecar with a concise change log from the legacy notebook to the corrected review candidate, unresolved evidence notes (if any), a human checklist for chronology/fairness/hindsight/sparse-visual review, and the explicit instruction that the user generates the PDF manually only after review approval. Create `docs/notebook/README.md` as a minimal pointer page that explains the preserved original notebook, the corrected review candidate, the evidence map, the review checklist, and the existing PDF generator. Update only the notebook navigation rows in `docs/INDEX.md` so the docs entry no longer points at a missing notebook path. Do not broaden this into a full docs reorganization.</action>
  <verify>
    <automated>source venv/bin/activate &amp;&amp; python scripts/verify_notebook_finalization.py --check checklist &amp;&amp; python scripts/verify_notebook_finalization.py --check packaging</automated>
  </verify>
  <done>The review bundle is navigable from `docs/`, the review checklist is explicit, and packaging no longer depends on a broken or undocumented path assumption.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human chronology and judge-readability signoff</name>
  <files>notebooks/P10_Research_Log_Notebook_Corrected.md, notebooks/P10_Research_Log_Notebook_Corrected_EVIDENCE_MAP.md, notebooks/P10_Research_Log_Notebook_Corrected_REVIEW.md, docs/notebook/README.md</files>
  <action>Use the completed review bundle to verify chronology, fairness, and judge readability before manual PDF export. This checkpoint exists because hindsight, sparse-visual discipline, and presentability cannot be proven reliably by static checks alone.</action>
  <what-built>The approval-anchored corrected notebook, evidence map, review checklist, and minimal docs packaging layer.</what-built>
  <how-to-verify>
    1. Read `notebooks/P10_Research_Log_Notebook_Corrected.md` and confirm the visible timeline starts at the approval-era anchor, stays chronological, and uses active-day headers plus brief gap notes.
    2. Cross-check 3-5 sensitive moments against `notebooks/P10_Research_Log_Notebook_Corrected_EVIDENCE_MAP.md`, especially final metrics, the TCN validation timing clarification, and any embedded figures.
    3. Compare the notebook flow against `docs/reference/Project S-19-05 Research Notebook (1).pdf` and confirm the result feels concise, judge-readable, and not paper-like.
    4. If the notebook is approved, run the existing manual export utility yourself: `source venv/bin/activate && python notebooks/generate_notebook_pdf.py`, then inspect the generated PDF for missing assets or formatting breakage.
  </how-to-verify>
  <resume-signal>Type "approved" to close Phase 4, or describe the review issues that must be fixed.</resume-signal>
  <verify>
    <automated>source venv/bin/activate &amp;&amp; python scripts/verify_notebook_finalization.py --full</automated>
  </verify>
  <done>The user has either approved the review bundle for manual PDF export or returned concrete review notes for one more revision pass.</done>
</task>

</tasks>

<verification>
Run the full verifier before the human checkpoint, then use the review sidecar to record any final issues instead of silently editing around them.</verification>

<success_criteria>
Phase 4 ends with a review-safe notebook package, explicit human signoff criteria, and a documented manual PDF step that preserves provenance and avoids in-place edits to the original notebook.</success_criteria>

<output>
After completion, create `.planning/phases/04-finalize-lab-notebook/04-finalize-lab-notebook-03-SUMMARY.md`
</output>
