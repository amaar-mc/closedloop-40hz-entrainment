---
phase: 04-finalize-lab-notebook
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - notebooks/P10_Research_Log_Notebook_Corrected.md
  - notebooks/P10_Research_Log_Notebook_Corrected_EVIDENCE_MAP.md
autonomous: true
requirements:
  - FNL-01
  - FNL-03
  - FNL-04
user_setup: []
must_haves:
  truths:
    - "The visible notebook timeline starts at the approval-era anchor instead of the December private-prep timeline."
    - "Every retained date-sensitive claim, metric, and pivotal figure is traceable to repository evidence."
    - "Chronology proceeds in ascending order with explicit gap notes where day-level evidence is thin."
  artifacts:
    - path: "notebooks/P10_Research_Log_Notebook_Corrected.md"
      provides: "Approval-anchored review candidate notebook"
    - path: "notebooks/P10_Research_Log_Notebook_Corrected_EVIDENCE_MAP.md"
      provides: "Whitelisted evidence map for claims, dates, and visuals"
  key_links:
    - from: "notebooks/P10_Research_Log_Notebook_Corrected_EVIDENCE_MAP.md"
      to: "notebooks/P10_Research_Log_Notebook_Corrected.md"
      via: "claim/date/figure disposition"
      pattern: "keep|rewrite|drop|gap note"
    - from: "notebooks/P10_Research_Log_Notebook_Corrected.md"
      to: "docs/submission/reports/PROJECT_ACHIEVEMENT_REPORT.md"
      via: "validated final metrics"
      pattern: "72\\.1%|82\\.6%|91%"
---

<objective>
Populate the evidence backbone and rewrite the corrected notebook candidate to the locked approval-era chronology.

Purpose: Turn the review bundle into a defensible notebook candidate that judges can trust without introducing hindsight or unsupported backfill.
Output: A populated evidence map and a corrected notebook draft anchored to Jan 15, 2026 unless stronger in-repo evidence tightens the date.
</objective>

<execution_context>
@/Users/amaarchughtai/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/amaarchughtai/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-finalize-lab-notebook/04-CONTEXT.md
@.planning/phases/04-finalize-lab-notebook/04-RESEARCH.md
@.planning/phases/04-finalize-lab-notebook/04-VALIDATION.md
@notebooks/P10_Lab_Notebook_FINAL.md
@archive/notebooks/LOG_NOTEBOOK.md
@archive/notebooks/LAB_NOTEBOOK_ERRATA.md
@docs/submission/reports/PROJECT_ACHIEVEMENT_REPORT.md
@docs/reference/PROJECT_DEEP_DIVE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Populate the claim and chronology evidence map</name>
  <files>notebooks/P10_Research_Log_Notebook_Corrected_EVIDENCE_MAP.md</files>
  <action>Replace the template with a real evidence inventory built from repository sources only. For each retained metric, date-sensitive milestone, and each planned embedded figure, record: the claim or event, the exact source file(s), the disposition (`keep`, `rewrite`, `drop`, `gap note`), and any correction note. Explicitly capture the locked Jan 15, 2026 fallback anchor, the Feb 21 vs Feb 26 validation clarification from `archive/notebooks/LAB_NOTEBOOK_ERRATA.md`, PAC-gap unit corrections, and the final quantitative claims from `docs/submission/reports/PROJECT_ACHIEVEMENT_REPORT.md`. If evidence is too weak for a daily claim, mark it as a gap note rather than inventing detail.</action>
  <verify>
    <automated>source venv/bin/activate &amp;&amp; python scripts/verify_notebook_finalization.py --check evidence</automated>
  </verify>
  <done>The evidence map is no longer a stub; it is the authoritative whitelist of what the corrected notebook may keep, rewrite, downgrade, or omit.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite the corrected notebook to the approval-anchored chronology</name>
  <files>notebooks/P10_Research_Log_Notebook_Corrected.md</files>
  <action>Rewrite the new corrected notebook from the existing notebook content and approved evidence map only. Start the visible timeline at `Jan 15, 2026` unless stronger in-repo evidence supports a tighter Jan 15-17 anchor. Compress pre-anchor material into brief background framing rather than daily entries. Use full date headers for active days, short gap notes between work clusters, and include planning/admin milestones only when they materially change research direction. Keep the hybrid style: concise paragraph narrative plus short structured blocks where they improve clarity. Maintain moderate technical depth by default, add deeper detail only on pivotal debugging, architecture pivots, validation, and breakthrough days, and include brief next-step rationale in each active-day entry. Do not add new science, do not backfill unsupported day-level detail, do not reveal later conclusions too early, and keep figures sparse by limiting them to pivotal days and major transitions. Update stale metrics and dates per the evidence map rather than silently carrying them over from the legacy notebook.</action>
  <verify>
    <automated>source venv/bin/activate &amp;&amp; python scripts/verify_notebook_finalization.py --check chronology &amp;&amp; python scripts/verify_notebook_finalization.py --check evidence</automated>
  </verify>
  <done>The corrected notebook reads like an approval-era daily research log, the visible dates are monotonic after the anchor, and every retained sensitive claim is backed by the evidence map.</done>
</task>

</tasks>

<verification>
Use the evidence map as the only allowed source-of-truth for chronology-sensitive edits, then run chronology and evidence checks together.</verification>

<success_criteria>
The corrected notebook is no longer a paper-like retrospective starting in December; it is an approval-anchored, evidence-backed review candidate built only from supported repository facts.</success_criteria>

<output>
After completion, create `.planning/phases/04-finalize-lab-notebook/04-finalize-lab-notebook-02-SUMMARY.md`
</output>
