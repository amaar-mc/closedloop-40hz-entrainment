---
phase: 04-finalize-lab-notebook
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/verify_notebook_finalization.py
  - notebooks/P10_Research_Log_Notebook_Corrected.md
  - notebooks/P10_Research_Log_Notebook_Corrected_REVIEW.md
  - notebooks/P10_Research_Log_Notebook_Corrected_EVIDENCE_MAP.md
autonomous: true
requirements:
  - FNL-02
  - FNL-06
user_setup: []
must_haves:
  truths:
    - "Original notebook artifacts remain untouched while a separate review bundle exists."
    - "The corrected notebook path matches the existing PDF generator contract."
    - "Automated sanity checks exist before editorial rewriting begins."
  artifacts:
    - path: "scripts/verify_notebook_finalization.py"
      provides: "Quick/full CLI checks for notebook finalization"
      exports: ["main"]
    - path: "notebooks/P10_Research_Log_Notebook_Corrected.md"
      provides: "New review-safe notebook source at generator path"
    - path: "notebooks/P10_Research_Log_Notebook_Corrected_REVIEW.md"
      provides: "Reviewer checklist and change-log scaffold"
    - path: "notebooks/P10_Research_Log_Notebook_Corrected_EVIDENCE_MAP.md"
      provides: "Evidence inventory scaffold for later claim tracing"
  key_links:
    - from: "notebooks/generate_notebook_pdf.py"
      to: "notebooks/P10_Research_Log_Notebook_Corrected.md"
      via: "hardcoded md_path"
      pattern: "P10_Research_Log_Notebook_Corrected\\.md"
    - from: "scripts/verify_notebook_finalization.py"
      to: "notebooks/P10_Research_Log_Notebook_Corrected_REVIEW.md"
      via: "--check checklist"
      pattern: "checklist"
---

<objective>
Create the non-destructive review bundle and Wave 0 verification scaffolding for notebook finalization.

Purpose: Lock in the safe file contract before any chronology rewrite so Phase 4 can proceed without touching the existing notebook in place.
Output: A corrected-source notebook stub, review/evidence sidecars, and a CLI verifier used by all later plans.
</objective>

<execution_context>
@/Users/amaarchughtai/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/amaarchughtai/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-finalize-lab-notebook/04-CONTEXT.md
@.planning/phases/04-finalize-lab-notebook/04-RESEARCH.md
@.planning/phases/04-finalize-lab-notebook/04-VALIDATION.md
@notebooks/P10_Lab_Notebook_FINAL.md
@notebooks/generate_notebook_pdf.py

<interfaces>
From `notebooks/generate_notebook_pdf.py`:

```python
md_path = "/Users/amaarchughtai/Developer/research/closedloop-40hz-entrainment/notebooks/P10_Research_Log_Notebook_Corrected.md"
pdf_path = "/Users/amaarchughtai/Developer/research/closedloop-40hz-entrainment/notebooks/P10_Research_Log_Notebook_Corrected.pdf"
sections_start = md_text.find("## Section 1:")
if sections_start == -1:
    sections_start = md_text.find("## Background")
```

The new corrected notebook must keep that exact filename and remain readable even if the generator starts at `## Background` instead of `## Section 1:`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Create Wave 0 notebook finalization verifier</name>
  <files>scripts/verify_notebook_finalization.py</files>
  <action>Create a new CLI audit script with `argparse` and repo-root discovery. Support `--quick`, `--full`, and `--check {chronology,preservation,evidence,checklist,packaging}`. Implement deterministic checks only: preservation must confirm `notebooks/P10_Lab_Notebook_FINAL.md` and `notebooks/P10_Lab_Notebook_FINAL.pdf` still exist and that the corrected files are distinct paths; checklist must confirm the review sidecar contains reviewer checklist sections and explicit manual-PDF instructions; evidence must confirm the evidence-map file contains structured claim/source/status entries; packaging must confirm the corrected markdown exists at the generator contract path and any referenced local files named in the review bundle exist; chronology should parse full date headers in the corrected notebook and enforce monotonic ordering once that file is populated later. Print `[PASS]` / `[FAIL]` style results and exit nonzero on any failing requested check.</action>
  <verify>
    <automated>source venv/bin/activate &amp;&amp; python -m py_compile scripts/verify_notebook_finalization.py</automated>
  </verify>
  <done>The verifier compiles, exposes the required CLI flags, and later plans can call the named checks without inventing new validation commands.</done>
</task>

<task type="auto">
  <name>Task 1: Establish the non-destructive review bundle</name>
  <files>notebooks/P10_Research_Log_Notebook_Corrected.md, notebooks/P10_Research_Log_Notebook_Corrected_REVIEW.md, notebooks/P10_Research_Log_Notebook_Corrected_EVIDENCE_MAP.md</files>
  <action>Create three new review files without modifying `notebooks/P10_Lab_Notebook_FINAL.md`. The corrected notebook should be a lightweight scaffold that preserves the generator filename contract, adds a clear review-candidate title block, and includes placeholder sections for approval anchor, active-day chronology, gap notes, and sparse figure placement. The review sidecar should include sections for change log, unresolved questions, human chronology/fairness review, and manual PDF export after approval. The evidence map should include a structured template for claim/date/figure entries with source paths and disposition (`keep`, `rewrite`, `drop`, or `gap note`). Do not move, rename, or delete any existing notebook artifacts.</action>
  <verify>
    <automated>source venv/bin/activate &amp;&amp; python scripts/verify_notebook_finalization.py --check preservation &amp;&amp; python scripts/verify_notebook_finalization.py --check checklist</automated>
  </verify>
  <done>A safe review bundle exists at new paths, the originals remain untouched, and later plans have concrete files to populate instead of editing the legacy notebook in place.</done>
</task>

</tasks>

<verification>
Run the verifier in targeted mode after each task and confirm the corrected notebook filename matches the existing PDF generator contract exactly.</verification>

<success_criteria>
Wave 0 exists, the corrected notebook bundle is created in `notebooks/`, and all later Phase 4 plans can validate against `scripts/verify_notebook_finalization.py` instead of manual guesswork.</success_criteria>

<output>
After completion, create `.planning/phases/04-finalize-lab-notebook/04-finalize-lab-notebook-01-SUMMARY.md`
</output>
