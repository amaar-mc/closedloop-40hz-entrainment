---
phase: 13-caregiver-app-and-pilot-preparation
plan: 03
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - docs/ELEVATOR_PITCH.md
  - docs/CLINICAL_ROADMAP.md
  - docs/flyer/FACILITY_FLYER.md
  - docs/flyer/facility_flyer.pdf
  - scripts/generate_qr_codes.py
  - scripts/generate_flyer_pdf.py
autonomous: false
requirements:
  - PRES-01
  - PRES-02
  - PRES-03
  - PRES-04

must_haves:
  truths:
    - "docs/ELEVATOR_PITCH.md exists with a 1-minute script framed as 'predictive model for neural state with temporal forecasting' — not 'AI for Alzheimer's'"
    - "docs/CLINICAL_ROADMAP.md exists covering integration path, clinical testing plan, remote monitoring vision, and hardware scaling (Muse → OpenBCI → clinical)"
    - "docs/flyer/facility_flyer.pdf is a print-ready one-page PDF with QR code placeholder, plain-language description, and contact info"
    - "scripts/generate_qr_codes.py accepts --app-url and --form-url args and produces PNG QR files in docs/flyer/"
    - "A Google Form for pilot feedback has been created and its URL is documented — QR code generated from that URL"
  artifacts:
    - path: "docs/ELEVATOR_PITCH.md"
      provides: "1-minute pitch script with Q&A handles"
      min_lines: 40
    - path: "docs/CLINICAL_ROADMAP.md"
      provides: "Clinical pathway document with hardware scaling, regulatory, and community benefit"
      min_lines: 80
    - path: "docs/flyer/FACILITY_FLYER.md"
      provides: "One-page flyer source with QR code position and plain-language copy"
      min_lines: 30
    - path: "docs/flyer/facility_flyer.pdf"
      provides: "Print-ready PDF for Mission Villa and Valley Medical leave-behind"
    - path: "scripts/generate_qr_codes.py"
      provides: "CLI script to regenerate QR PNGs from app and form URLs"
      contains: "argparse"
  key_links:
    - from: "docs/flyer/facility_flyer.pdf"
      to: "QR code PNG"
      via: "scripts/generate_flyer_pdf.py embeds QR image"
      pattern: "qrcode|QRCode"
    - from: "scripts/generate_qr_codes.py"
      to: "--app-url arg"
      via: "qrcode.QRCode.add_data(url)"
      pattern: "add_data"
---

<objective>
Create all CSEF presentation and pilot preparation materials: elevator pitch script, clinical
roadmap document, print-ready facility flyer PDF with QR codes, and pilot feedback Google Form.

Purpose: Judges need a coherent narrative beyond the demo — a clear explanation of the research
contribution, a realistic clinical path, and evidence of real-world interest (facility flyer + Form).
Output: docs/ELEVATOR_PITCH.md, docs/CLINICAL_ROADMAP.md, docs/flyer/ (PDF + QR codes),
scripts/generate_qr_codes.py, scripts/generate_flyer_pdf.py
</objective>

<execution_context>
@/Users/amaarchughtai/.claude/get-shit-done/workflows/execute-plan.md
@/Users/amaarchughtai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-caregiver-app-and-pilot-preparation/13-RESEARCH.md
@docs/presentations/01_main_script.md
@docs/poster/POSTER_BOARD_V5.md
@results/RESULTS_REPORT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Elevator pitch, clinical roadmap, and QR generation script</name>
  <files>docs/ELEVATOR_PITCH.md, docs/CLINICAL_ROADMAP.md, scripts/generate_qr_codes.py</files>
  <action>
**Create docs/ELEVATOR_PITCH.md** — a 1-minute spoken script (~130 words at normal pace).

Frame as "predictive model for neural state" — NOT "AI cures Alzheimer's". Key beats:

1. Hook (10s): "40 Hz gamma oscillations are disrupted in Alzheimer's patients. Restoring them
   through non-invasive sound stimulation has shown promising results in animal models."
2. Problem (15s): "But current systems are reactive — they stimulate and wait. By the time the
   brain responds, the optimal window may have passed."
3. Solution (20s): "I built a causal temporal convolutional network that predicts where your
   neural state will be 5 to 10 seconds in the future — with R² of 0.25 where simpler baselines
   go negative. The system uses that forecast to stimulate proactively, when it's most effective."
4. Results (15s): "On 35 subjects' real EEG data, the predictive strategy achieved 72% targeting
   accuracy versus 64% for reactive control — a statistically significant improvement."
5. Demo + Next Steps (10s): "Scan the QR code to try the live app. The next step is a formal
   pilot at a memory care facility to collect real caregiver feedback."

Include a "Q&A Quick Handles" section with 3 prepared answers:
- "Is this FDA approved?" → "No, this is research-stage. The roadmap includes IND filing and
  IRB-approved clinical trials after validating on a larger cohort."
- "Does it actually cure Alzheimer's?" → "We're not claiming a cure. We're restoring a
  disrupted neural rhythm that multiple studies associate with cognitive function. Think of it
  as a hearing aid for the brain's synchrony."
- "Why consumer EEG?" → "It makes the system accessible. Research-grade hardware is in the
  roadmap. The intelligence layer is the same; only the sensor changes."

**Create docs/CLINICAL_ROADMAP.md** — structured clinical pathway document covering:

Section 1 — Current State: 4-channel Muse 2 demo, simulated EEG for safe deployment, N=35
retrospective validation, R² 0.25–0.28 at 5–10s horizons.

Section 2 — Integration Path: How the system fits alongside existing neurofeedback and cognitive
therapy programs at memory care facilities. Framing: "decision support for therapy scheduling,
not autonomous treatment." Emphasizes caregiver control.

Section 3 — Clinical Testing Plan (3 phases):
- Phase A: Observational (no stimulation) — 10 participants, 2 weeks, collect EEG baselines
- Phase B: Feasibility (controlled stimulation) — IRB approval required, 20 participants
- Phase C: Comparative (predictive vs reactive) — 50 participants, primary outcome MMSE score

Section 4 — Remote Monitoring Vision: Caregiver app → cloud dashboard → clinician review portal.
Real-time alerts for anomalous PAC trends. Longitudinal session history analytics.

Section 5 — Hardware Scaling Path:
- Current: Muse 2 (4 dry electrodes, consumer grade, $200)
- Next: OpenBCI Cyton (8 channels, gel electrodes, 24-bit resolution, $500) — gamma SNR ×4
- Clinical: Research-grade 64-channel cap (EGI/BrainProducts, hospital setting)
- Each tier: same software stack, only sensor layer changes

Section 6 — Community Benefit Narrative: 6.7 million Americans with Alzheimer's, $321 billion
annual cost, 11 million unpaid caregivers. The system targets the caregiver workflow specifically
— reducing guesswork in therapy scheduling. Facility partnerships (Mission Villa, Valley Medical).

**Create scripts/generate_qr_codes.py** — CLI script:

```python
#!/usr/bin/env python3
"""
Generate QR code PNGs for the caregiver app URL and pilot feedback Google Form URL.

Usage:
    python scripts/generate_qr_codes.py --app-url URL --form-url URL [--output-dir PATH]
    python scripts/generate_qr_codes.py --check   # verify output files exist

Outputs:
    docs/flyer/qr_app.png       — QR code for the Streamlit app
    docs/flyer/qr_feedback.png  — QR code for the pilot feedback Google Form
"""
import argparse
import sys
from pathlib import Path

REPO_ROOT = Path(__file__).resolve().parent.parent
DEFAULT_OUTPUT = REPO_ROOT / "docs" / "flyer"


def make_qr(url: str, output_path: Path) -> None:
    import qrcode
    qr = qrcode.QRCode(
        version=1,
        error_correction=qrcode.constants.ERROR_CORRECT_L,
        box_size=10,
        border=4,
    )
    qr.add_data(url)
    qr.make(fit=True)
    img = qr.make_image(fill_color="black", back_color="white")
    output_path.parent.mkdir(parents=True, exist_ok=True)
    img.save(str(output_path))
    print(f"[PASS] QR saved: {output_path}")


def main() -> None:
    parser = argparse.ArgumentParser(description="Generate QR code PNGs")
    parser.add_argument("--app-url", default="", help="Streamlit app URL")
    parser.add_argument("--form-url", default="", help="Google Form URL")
    parser.add_argument("--output-dir", default=str(DEFAULT_OUTPUT), help="Output directory")
    parser.add_argument("--check", action="store_true", help="Check output files exist")
    args = parser.parse_args()

    out_dir = Path(args.output_dir)
    app_qr = out_dir / "qr_app.png"
    form_qr = out_dir / "qr_feedback.png"

    if args.check:
        missing = [str(p) for p in [app_qr, form_qr] if not p.exists()]
        if missing:
            print(f"[FAIL] Missing QR files: {missing}")
            raise SystemExit(1)
        print("[PASS] QR files present")
        raise SystemExit(0)

    if not args.app_url and not args.form_url:
        print("[WARN] No URLs provided. Use --app-url and --form-url.")
        raise SystemExit(1)

    if args.app_url:
        make_qr(args.app_url, app_qr)
    if args.form_url:
        make_qr(args.form_url, form_qr)


if __name__ == "__main__":
    main()
```
  </action>
  <verify>
    <automated>cd /Users/amaarchughtai/Developer/research/closedloop-40hz-entrainment && python -m py_compile scripts/generate_qr_codes.py && ls docs/ELEVATOR_PITCH.md && ls docs/CLINICAL_ROADMAP.md && wc -l docs/ELEVATOR_PITCH.md docs/CLINICAL_ROADMAP.md | awk 'NR==1{if($1<40) exit 1} NR==2{if($1<80) exit 1}' && echo "[PASS] docs line counts OK" || echo "[FAIL] docs too short"</automated>
  </verify>
  <done>docs/ELEVATOR_PITCH.md exists with 40+ lines including Q&A handles. docs/CLINICAL_ROADMAP.md exists with 80+ lines covering all 6 sections. scripts/generate_qr_codes.py compiles and accepts --app-url, --form-url, --check args.</done>
</task>

<task type="auto">
  <name>Task 2: Facility flyer PDF and pilot feedback form setup</name>
  <files>docs/flyer/FACILITY_FLYER.md, docs/flyer/facility_flyer.pdf, scripts/generate_flyer_pdf.py</files>
  <action>
**Create docs/flyer/FACILITY_FLYER.md** — one-page flyer source content:

```markdown
# NeuroCare 40Hz — Personalized Neural Entrainment Therapy

## What It Does
Gently restores 40 Hz brainwave patterns disrupted in Alzheimer's and MCI patients
using non-invasive sound stimulation — automatically adjusted to each patient's brain state.

## How It Works
1. Patient wears lightweight EEG headset during therapy session
2. System continuously reads brain activity
3. AI predicts optimal stimulation timing 5–10 seconds ahead
4. 40 Hz audio stimulus activates when brain synchrony is lowest

## Why It Matters
- Non-invasive: no drugs, no implants, no side effects
- Personalized: each session adapts to the patient's unique response
- Evidence-based: validated on EEG recordings from 35 participants
- Proactive: predicts the right moment before the window passes

## Results
72% targeting accuracy (vs 64% reactive baseline) on held-out subjects

## Try the Live Demo
[QR CODE PLACEHOLDER — qr_app.png]
Scan to open the caregiver app on your phone

## Share Your Feedback
[QR CODE PLACEHOLDER — qr_feedback.png]
Your input shapes the next phase of clinical development

## Contact
Amaar Chughtai · amaardevx@gmail.com
Research presented at CSEF 2026
```

**Create scripts/generate_flyer_pdf.py** — generates docs/flyer/facility_flyer.pdf using
reportlab. The PDF must be letter-size (8.5×11 in), print-ready (300 DPI effective), clean
single-column layout. Embed both QR images if they exist at docs/flyer/qr_app.png and
docs/flyer/qr_feedback.png; otherwise place grey placeholder boxes labeled "QR: App URL" and
"QR: Feedback Form".

```python
#!/usr/bin/env python3
"""
Generate print-ready facility flyer PDF using reportlab.

Usage:
    python scripts/generate_flyer_pdf.py [--output PATH]

Output:
    docs/flyer/facility_flyer.pdf
"""
import argparse
from pathlib import Path

REPO_ROOT = Path(__file__).resolve().parent.parent
DEFAULT_OUTPUT = REPO_ROOT / "docs" / "flyer" / "facility_flyer.pdf"
QR_APP = REPO_ROOT / "docs" / "flyer" / "qr_app.png"
QR_FORM = REPO_ROOT / "docs" / "flyer" / "qr_feedback.png"


def build_pdf(output_path: Path) -> None:
    from reportlab.lib.pagesizes import letter
    from reportlab.lib.units import inch
    from reportlab.lib.styles import getSampleStyleSheet, ParagraphStyle
    from reportlab.lib.enums import TA_CENTER, TA_LEFT
    from reportlab.lib import colors
    from reportlab.platypus import SimpleDocTemplate, Paragraph, Spacer, Image, Table, TableStyle
    from reportlab.lib.colors import HexColor

    output_path.parent.mkdir(parents=True, exist_ok=True)
    doc = SimpleDocTemplate(
        str(output_path),
        pagesize=letter,
        rightMargin=0.75 * inch,
        leftMargin=0.75 * inch,
        topMargin=0.75 * inch,
        bottomMargin=0.75 * inch,
    )

    styles = getSampleStyleSheet()
    title_style = ParagraphStyle(
        "Title",
        parent=styles["Title"],
        fontSize=22,
        textColor=HexColor("#1f77b4"),
        spaceAfter=6,
        alignment=TA_CENTER,
    )
    heading_style = ParagraphStyle(
        "Heading",
        parent=styles["Heading2"],
        fontSize=13,
        textColor=HexColor("#1f77b4"),
        spaceBefore=12,
        spaceAfter=4,
    )
    body_style = ParagraphStyle(
        "Body",
        parent=styles["Normal"],
        fontSize=10,
        leading=14,
        spaceAfter=4,
    )
    caption_style = ParagraphStyle(
        "Caption",
        parent=styles["Normal"],
        fontSize=9,
        alignment=TA_CENTER,
        textColor=colors.grey,
    )

    story = []

    story.append(Paragraph("NeuroCare 40Hz", title_style))
    story.append(Paragraph("Personalized Neural Entrainment Therapy", heading_style))
    story.append(Spacer(1, 0.1 * inch))

    story.append(Paragraph("What It Does", heading_style))
    story.append(Paragraph(
        "Gently restores 40 Hz brainwave patterns disrupted in Alzheimer's and MCI patients "
        "using non-invasive sound stimulation — automatically adjusted to each patient's brain state.",
        body_style
    ))

    story.append(Paragraph("How It Works", heading_style))
    steps = [
        "Patient wears lightweight EEG headset during therapy session",
        "System continuously reads brain activity in real time",
        "AI predicts optimal stimulation timing 5–10 seconds ahead",
        "40 Hz audio stimulus activates when brain synchrony is lowest",
    ]
    for i, step in enumerate(steps, 1):
        story.append(Paragraph(f"{i}. {step}", body_style))

    story.append(Paragraph("Why It Matters", heading_style))
    bullets = [
        "Non-invasive: no drugs, no implants, no side effects",
        "Personalized: each session adapts to the patient's unique response",
        "Evidence-based: validated on EEG recordings from 35 participants",
        "Proactive: predicts the right moment before the window passes",
    ]
    for b in bullets:
        story.append(Paragraph(f"• {b}", body_style))

    story.append(Paragraph("Key Result", heading_style))
    story.append(Paragraph(
        "<b>72% targeting accuracy</b> vs 64% reactive baseline — "
        "statistically significant improvement across 35 subjects (Cohen's d = 1.31, p < 0.001)",
        body_style
    ))

    # QR code section — two columns
    story.append(Spacer(1, 0.15 * inch))
    qr_size = 1.4 * inch

    def qr_cell(qr_path: Path, caption: str):
        if qr_path.exists():
            return [Image(str(qr_path), width=qr_size, height=qr_size),
                    Paragraph(caption, caption_style)]
        else:
            placeholder = Paragraph(f"[QR: {caption}]", caption_style)
            return [Spacer(qr_size, qr_size), placeholder]

    app_cell = qr_cell(QR_APP, "Scan to try the live demo")
    form_cell = qr_cell(QR_FORM, "Share your feedback")

    table = Table(
        [[app_cell[0], form_cell[0]], [app_cell[1], form_cell[1]]],
        colWidths=[3.5 * inch, 3.5 * inch],
    )
    table.setStyle(TableStyle([
        ("ALIGN", (0, 0), (-1, -1), "CENTER"),
        ("VALIGN", (0, 0), (-1, -1), "MIDDLE"),
    ]))
    story.append(table)

    story.append(Spacer(1, 0.15 * inch))
    story.append(Paragraph(
        "Amaar Chughtai · amaardevx@gmail.com · CSEF 2026",
        caption_style
    ))

    doc.build(story)
    print(f"[PASS] Flyer PDF generated: {output_path}")


def main() -> None:
    parser = argparse.ArgumentParser(description="Generate facility flyer PDF")
    parser.add_argument("--output", default=str(DEFAULT_OUTPUT), help="Output PDF path")
    args = parser.parse_args()
    build_pdf(Path(args.output))


if __name__ == "__main__":
    main()
```

Run the script to generate the PDF:
```bash
cd /Users/amaarchughtai/Developer/research/closedloop-40hz-entrainment
pip install qrcode[pil] reportlab -q
python scripts/generate_flyer_pdf.py
```

**Pilot feedback Google Form — HUMAN STEP (documented below in checkpoint).**

For PRES-04: After the Google Form is created in the checkpoint, the executor should run:
```bash
python scripts/generate_qr_codes.py \
  --app-url "https://PLACEHOLDER.streamlit.app" \
  --form-url "https://forms.gle/PLACEHOLDER"
```
The URLs will be filled in by the human checkpoint response. For now, generate a placeholder
QR for a test URL so the script is verified to work:
```bash
python scripts/generate_qr_codes.py \
  --app-url "https://example.streamlit.app" \
  --form-url "https://forms.google.com/placeholder"
```
  </action>
  <verify>
    <automated>cd /Users/amaarchughtai/Developer/research/closedloop-40hz-entrainment && python -m py_compile scripts/generate_flyer_pdf.py && ls docs/flyer/facility_flyer.pdf && ls docs/flyer/qr_app.png && ls docs/flyer/qr_feedback.png && python -c "import os; s=os.path.getsize('docs/flyer/facility_flyer.pdf'); assert s > 5000, f'PDF too small: {s} bytes'; print(f'[PASS] PDF size: {s} bytes')"</automated>
  </verify>
  <done>docs/flyer/facility_flyer.pdf exists and is larger than 5 KB (valid PDF). docs/flyer/qr_app.png and qr_feedback.png exist. scripts/generate_flyer_pdf.py compiles. FACILITY_FLYER.md source exists.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Review materials, deploy to Streamlit Cloud, create Google Form, regenerate QR codes</name>
  <what-built>Elevator pitch, clinical roadmap, facility flyer PDF with QR placeholders, QR generation script. All presentation materials are complete except for real app URL and Google Form URL.</what-built>
  <how-to-verify>
1. **Review docs/ELEVATOR_PITCH.md** — read the script aloud. Should take ~60 seconds. Check that it mentions "temporal forecasting" and "R² 0.25", NOT "AI cures Alzheimer's".

2. **Review docs/CLINICAL_ROADMAP.md** — confirm it covers: integration path, 3-phase clinical testing plan, remote monitoring, hardware scaling (Muse → OpenBCI → clinical), community benefit.

3. **Review the facility flyer PDF** — open docs/flyer/facility_flyer.pdf. Confirm it's one page, readable, and ready to print. QR codes will show placeholder boxes (real URLs added next).

4. **Create the pilot feedback Google Form** at https://forms.google.com with questions:
   - Patient/caregiver role (dropdown: Caregiver / Nurse / Facility Admin / Judge / Other)
   - "How easy was the app to use?" (1-5 rating)
   - "Would you use this system in a therapy session?" (Yes/No/Maybe)
   - "What would you improve?" (paragraph)
   - "May we follow up with you?" (Yes/No + optional email)
   Submit a test response to confirm it works.

5. **Deploy the app to Streamlit Cloud**:
   - Go to https://share.streamlit.io → Connect GitHub repo → set Main file: caregiver_app.py
   - Choose a custom subdomain like "neurocare40hz" → get URL: https://neurocare40hz.streamlit.app
   - Wait for deploy to succeed (model .pth files are <200 KB total — no LFS needed)
   - Scan the app URL on your phone to confirm it loads

6. **Regenerate QR codes with real URLs**:
   ```bash
   python scripts/generate_qr_codes.py \
     --app-url "PASTE_REAL_STREAMLIT_URL" \
     --form-url "PASTE_REAL_FORM_URL"
   ```
   Then regenerate the flyer:
   ```bash
   python scripts/generate_flyer_pdf.py
   ```
  </how-to-verify>
  <resume-signal>Type "approved" with the real Streamlit app URL and Google Form URL so they can be recorded in the SUMMARY, or describe any issues found</resume-signal>
  <action>Human verification — review all docs, deploy to Streamlit Cloud, create Google Form, regenerate QR codes with real URLs, regenerate flyer PDF.</action>
  <verify><automated>ls docs/ELEVATOR_PITCH.md docs/CLINICAL_ROADMAP.md docs/flyer/facility_flyer.pdf docs/flyer/qr_app.png docs/flyer/qr_feedback.png</automated></verify>
  <done>App deployed to Streamlit Cloud with working URL. Google Form created with at least one test response. QR codes regenerated with real URLs. Flyer PDF regenerated with embedded QR codes.</done>
</task>

</tasks>

<verification>
After all tasks and checkpoint complete:
```bash
cd /Users/amaarchughtai/Developer/research/closedloop-40hz-entrainment
# Docs exist and meet minimum line counts
wc -l docs/ELEVATOR_PITCH.md docs/CLINICAL_ROADMAP.md
ls docs/flyer/FACILITY_FLYER.md docs/flyer/facility_flyer.pdf
ls docs/flyer/qr_app.png docs/flyer/qr_feedback.png
# Scripts compile
python -m py_compile scripts/generate_qr_codes.py scripts/generate_flyer_pdf.py
# PDF is valid
python -c "
import os
pdf_size = os.path.getsize('docs/flyer/facility_flyer.pdf')
assert pdf_size > 5000, f'PDF too small: {pdf_size} bytes'
print(f'[PASS] facility_flyer.pdf: {pdf_size} bytes')
"
# QR files are non-empty PNGs
python -c "
from pathlib import Path
for f in ['docs/flyer/qr_app.png', 'docs/flyer/qr_feedback.png']:
    size = Path(f).stat().st_size
    assert size > 500, f'{f} too small: {size} bytes'
    print(f'[PASS] {f}: {size} bytes')
"
```
</verification>

<success_criteria>
- docs/ELEVATOR_PITCH.md: 40+ lines, "temporal forecasting" and "R² 0.25" mentioned, Q&A handles present
- docs/CLINICAL_ROADMAP.md: 80+ lines, covers all 6 sections
- docs/flyer/FACILITY_FLYER.md: source content exists
- docs/flyer/facility_flyer.pdf: valid PDF > 5 KB, print-ready one-page layout
- docs/flyer/qr_app.png and qr_feedback.png: non-empty PNG files
- scripts/generate_qr_codes.py and generate_flyer_pdf.py: compile cleanly and run
- Human checkpoint: App deployed to Streamlit Cloud, Google Form created with test response, real QR codes regenerated with live URLs
</success_criteria>

<output>
After completion, create `.planning/phases/13-caregiver-app-and-pilot-preparation/13-03-SUMMARY.md`
</output>
