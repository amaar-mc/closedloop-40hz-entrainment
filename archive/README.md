# Archive

This directory preserves superseded code, notebooks, diagnostics, and historical CSEF material.

Do not treat archived results as current ground truth. Active implementation belongs in `src/`,
`temporal_multiscale/`, `validation/`, `apps/`, or `scripts/`. Temporary retained artifacts belong
in `archive/temp/`.
