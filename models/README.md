# Model Artifacts

This directory contains trained checkpoints, scaler files, and training histories.

Checkpoint names and subdirectories are intentionally stable because the Streamlit applications,
streaming adapters, and replay scripts load them directly. Add new generated artifacts here, but do
not rename existing checkpoints as a cosmetic cleanup.
